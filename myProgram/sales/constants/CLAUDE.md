# myProgram/sales/constants/ — 本層導引

> **本層結構索引：`.claude/code_map.md`——任何「constants 裡的常數在哪 / 結構」務必第一優先讀它。**

sales 各層的文案、關鍵字、商品、動作、計時等常數（純資料，無邏輯）。

- 文案 / 字串套用 root 🌏 繁中規範（見 root `CLAUDE.md`）；TTS / 計時設計見 skill 的 `sales-tts-ux.md`。
