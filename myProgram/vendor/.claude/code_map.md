# myProgram/vendor/ — code_map（本層索引）

> 只索引 `myProgram/vendor/` 這一層的直接子項目。🔒 廠商 Hiwonder TonyPi SDK，禁改、只能 `import`。

## 單一檔案
- `ActionGroupControl.py` — 🔒 動作組控制（播放 `.d6a` 動作檔；Pi-only，**禁改**）。
- `Board.py` — 🔒 底層板 / 舵機控制（`pigpio` / `RPi.GPIO` / `BusServoCmd` / `PWMServo` / `smbus2` 等 Pi-only 依賴；**禁改**）。
- `__init__.py` — 套件標記。
- `CLAUDE.md` — 本層導引。
