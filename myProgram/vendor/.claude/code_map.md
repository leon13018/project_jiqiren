# myProgram/vendor/ — code_map（本層索引，葉層）

> 本層索引：只列 `myProgram/vendor/` 直接子項目，一行一項。顆粒：細。
> 🔒 廠商 Hiwonder TonyPi SDK，禁改、只能 `import`。

## 子目錄
- `.claude/` — 本層 CC 配置（`code_map.md` 本檔）。

## 檔案
- `ActionGroupControl.py` — 🔒 動作組控制（播放 `.d6a` 動作檔；Pi-only，**禁改**）。
- `Board.py` — 🔒 底層板 / 舵機控制（`pigpio` / `RPi.GPIO` / `BusServoCmd` / `PWMServo` / `smbus2` 等 Pi-only 依賴；**禁改**）。
- `__init__.py` — 套件標記。
- `CLAUDE.md` — 本層導引。
