# perf_w4 — sweep（entry-prompt helper＋死常數移除）（spec — WHAT）

> perf campaign Phase 3 第三波（sweep）。傘狀：`resources/specs/perf_review_2026-06-12_design.md`；
> findings：F-8（P2）＋F-12（P2，user 已明示同意連測試移除）＋F-13（評估後不採，見 §4）。
> F-9a/F-10/F-11 已於 perf_w1 落地；驗收＝509 全綠＋bench 無回歸。

## 1. 背景與動機

**F-8**：`L2_ENTRY_PROMPT if cart_module.is_empty(...) else L3_ENTRY_PROMPT` 同字面三元式
出現兩處（`l2_l3_dialog.py:152` run_dialog 進場、`:323` reenter）——「cart 世界狀態 →
entry prompt」的映射應單一事實來源。

**F-12**：`AUTO_CHECKOUT_NOTICE = 12`（timing.py:71）prod 零引用——C-2 第二段已改用
`C2_DECISION_TIMEOUT = 6`（2026-05-28 重設計）。殘留引用僅：`test_constants.py:32`
守值斷言、`test_states.py:29` **未使用的 import**、兩檔過時註解。主 agent 已核實
`tests/spec/` 的命中全為歷史場景文檔（不動）。

## 2. 設計核心與行為規約

**行為 100% 不變**；測試總數不變（509——F-12 移除的是多斷言測試內的單一 assert 與
未使用 import，無測試函式增刪）。

### 2-1. F-8：`_entry_prompt_for` helper（`l2_l3_dialog.py`，置於 `run_dialog` 之前）

```python
def _entry_prompt_for(cart) -> str:
    """cart 世界狀態 → entry prompt（空 → L2、非空 → L3）；進場與 reenter 共用。"""
    return L2_ENTRY_PROMPT if cart_module.is_empty(cart) else L3_ENTRY_PROMPT
```

兩 call site 改用；`:149` 相鄰的 `ACTION_L2/ACTION_L3` 三元式**不動**（不在 findings，
surgical 邊界）。ModePolicy 的 `entry_prompt` class attr 是另一機制（policy 靜態屬性），
不動。

### 2-2. F-12：死常數連衛兵移除

| 檔 | 改動 |
|---|---|
| `constants/timing.py` | 刪 `AUTO_CHECKOUT_NOTICE: int = 12` 與其上 3 行註解；`__all__` 移除該項；模組 docstring 清單移除該名；`C2_DECISION_TIMEOUT` 註解「從 AUTO_CHECKOUT_NOTICE=12 縮短為 6」改「自原 C-2 嚴格 yes/no 12s 設計縮短為 6」（保留歷史語意、不引用已刪名） |
| `tests/sales/test_constants.py` | 刪 `assert const.AUTO_CHECKOUT_NOTICE == 12`；scenario 註解（:22-25）移除該常數敘述（OPENCV_MUTE 部分保留） |
| `tests/sales/test_states.py` | 刪 import 清單中 `AUTO_CHECKOUT_NOTICE,`（:29，未使用）；過時 scenario 註解（:2865）「AUTO_CHECKOUT_NOTICE（10）秒」改「C2_DECISION_TIMEOUT（6）秒」（對齊現行為） |

## 3. 改檔範圍（高層）

| 檔 | 改動 | 行數估計 |
|---|---|---|
| `myProgram/sales/states/l2_l3_dialog.py` | `_entry_prompt_for`＋兩 call site | +5/-2 |
| `myProgram/sales/constants/timing.py` | 刪常數＋註解調整 | -6/±2 |
| `tests/sales/test_constants.py` | 刪 assert＋註解調整 | -3 |
| `tests/sales/test_states.py` | 刪未用 import＋註解修正 | -1/±1 |

無檔案增刪搬移 → 不觸發 code_map 更新。

## 4. Out of scope

- **F-13（timed_call 計時包裝）評估後不採**：兩處 pattern 各僅 2 行 `monotonic()` 量測、
  補償動作（單 deadline 就地 vs 雙 deadline 回傳）無法共用——抽取屬 premature
  abstraction（2 站點 × 2 行），收益不抵間接層；記錄為已評估，防重提。
- `tests/spec/` 歷史場景文檔的 AUTO_CHECKOUT_NOTICE 字樣（歷史存檔不回填）。
- `:149` 的 ACTION 三元式、ModePolicy entry_prompt 屬性。
- F-4/F-5（TTS prefetch）→ perf_w2。

## 5. 規範與參考

派 sales-coder；純重構規約（綠→改→綠→commit）；F-12 屬「死碼＋衛兵移除」，
測試改動為伴隨調整（user 2026-06-12 裁決明示同意）。

## 6. 測試指令＋預期結果

1. 每 commit 後：`python -m pytest tests/sales/` → **`509 passed`**（無測試函式增刪）。
2. `python -m tests.perf.bench_sales --smoke` → 7 場景全過。
3. 收尾 bench 3 輪 vs perf_w3 後：全項 |差| < 10%（sweep 波無回歸即過）。
4. grep 驗證：`AUTO_CHECKOUT_NOTICE` 在 `myProgram/` 與 `tests/sales/` 0 命中
   （`tests/spec/` 歷史檔允許殘留）。

## 7. Commit 規範（依序）

| # | 內容 | message |
|---|---|---|
| 1 | F-8 | `refactor(sales): extract _entry_prompt_for shared by dialog entry and reenter` |
| 2 | F-12 | `refactor(sales): drop dead AUTO_CHECKOUT_NOTICE constant and its test guards` |

git add 明列檔名；Co-Authored-By: Claude Opus。

## 8. 流程鳥瞰

```
worktree-perf-w4（已建，基於 4bb7465）
  └─ spec/plan approval → commit docs → sales-coder（2 commits）
     → 主 agent Iron Law（pytest 509＋smoke＋grep＋diff --stat＋branch）
     → spec-reviewer(sonnet) → code-quality-reviewer(opus)
     → git -C 主checkout ff-merge → push → bench 3 輪無回歸
     →（cwd-pinned）worktree/branch 清理延後新 session
```

---

## 實測結果（2026-06-12 收尾 append，Iron Law 證據）

- bench 3 輪 vs perf_w3 後：代表項中位數全在 ±4% 內（classify_intent 無命中 7.2~7.6µs、
  L2 點餐劇本 22.6~23.7µs、L3 結帳 6.9~7.2µs），<10% 閾值——sweep 零效能代價。
- pytest `509 passed`（測試函式零增刪）；smoke 7 場景全過；grep `AUTO_CHECKOUT_NOTICE`
  在 myProgram/＋tests/sales/ 0 命中。
- 三段審查：spec-reviewer ✅；code-quality ✅ Approved（零 Critical/Important）。
- 主 agent 追加：`test_states.py:2861` 標題行過時「10 秒」一併修正（`2e07ba9`）。
- **場外觀察記錄（code-quality reviewer 提出，非本波範圍）**：`C2_DECISION_TIMEOUT`
  （活常數）在 test_constants 無 `== 6` 守值斷言——既有缺口非本波引入；
  若日後補衛兵覆蓋面，列為後續 finding 候選。
