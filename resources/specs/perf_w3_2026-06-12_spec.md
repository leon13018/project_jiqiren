# perf_w3 — 數量分類收斂＋cancel-gate helper（spec — WHAT）

> perf campaign Phase 3 第二波。傘狀設計：`resources/specs/perf_review_2026-06-12_design.md`；
> findings：`resources/reviews/full_review_2026-06-12.md` F-6（P1）＋F-7（P2，user A 全採）。
> 兩項皆冷路徑「簡潔結構贏」治理；驗收重點是**504 回歸網全綠＋bench 無回歸**，不期待變快。

## 1. 背景與動機

**F-6**：「remaining_capacity → at-cap / zero / over_limit / ok」同一決策邊界手寫三份：
`_l2_l3_qty_followup.py` Pass 1（:121-141）、qty 追問 sub-loop（:228-251）、
`_invalid_qty_reask._classify_into_pending`（:84-93）。改規則要記得同步三檔，已是漂移風險。

**F-7**：`_dialog_checkout_confirm`（:666-671）與 `_dialog_unclear_final_confirmation`
（:742-750）的 cancel-gate 區塊近逐行相同（`CANCEL_CONFIRM.run` → YES 退出 / NO speak
DECLINED＋重 prompt＋continue 且不計 unclear）。「NO 後的行為」應單一事實來源。

## 2. 設計核心與行為規約

**行為 100% 不變**；既有 504 測試零修改；新增 5 個 classify_qty 單元測試（TDD）→ 預期 509。

### 2-1. F-6：`cart.classify_qty` 純函式（新增於 `cart.py`，`remaining_capacity` 之後）

```python
def classify_qty(cart: Cart, product: str, qty: int) -> str:
    """數量四分類（perf_w3 F-6 收斂三處手寫副本）。

    判定順序即優先序（對齊 Pass 1 既有行序）：
        "at_cap"     — 該商品 cart 內已達單筆上限（remaining <= 0），與 qty 無關
        "zero"       — qty == 0（顧客明確說 0）
        "over_limit" — qty > 剩餘可加量
        "ok"         — 0 < qty <= 剩餘可加量
    純分類不動 cart；動作（add / pending / funnel / speak）由 caller 決定。
    """
    remaining = remaining_capacity(cart, product)
    if remaining <= 0:
        return "at_cap"
    if qty == 0:
        return "zero"
    if qty > remaining:
        return "over_limit"
    return "ok"
```

選 `cart.py` 落點理由：分類完全由 cart 容量狀態決定、三 caller 都已 import
`cart as cart_module`（零新依賴邊）、維持純函式契約。回傳字串刻意與既有 pending
reason 值（`"zero"`/`"over_limit"`）同字面——caller 可直接 `pending[product] = verdict`。

**三 call site 行為等價對照**（關鍵差異與處理）：

| Site | 既有行序 | 等價性 |
|---|---|---|
| Pass 1 | at_cap → zero → over_limit → ok | 與 classify_qty 同序，逐字等價 |
| sub-loop | at_cap → (zero\|over) funnel → ok | `reason = verdict` 直代（同字面） |
| `_classify_into_pending` | ok → zero → else over_limit（**無 at_cap 分支**） | qty==0 且 remaining<=0 時舊行為「zero」vs classify_qty「at_cap」——**此態不可達**：pending 商品在 Pass 1 已保證 remaining>0（at-cap 在進 pending 前被攔），重問期間該商品唯一加量路徑是成功 add（隨即移出 pending），remaining 不會中途歸零。防禦性處理：caller 把 `"at_cap"` 映射為 `"over_limit"`（對齊舊 else 分支），不可達態也不改行為 |

### 2-2. F-7：`_cancel_gate` helper（`l2_l3_dialog.py`，置於 `_dialog_checkout_confirm` 之前）

```python
def _cancel_gate(io, reprompt: str) -> bool:
    """確認子狀態共用 cancel gate（perf_w3 F-7 收斂兩處逐行重複）。

    跑 CANCEL_CONFIRM 單例：
        True  — 顧客確認取消；caller 自行退出（checkout_confirm 回 "cancel_to_l1"、
                unclear_final 走 session.exit_a()——兩者退出語意不同，不入 helper）
        False — 拒絕取消；已 speak CANCEL_DECLINED_NOTICE＋重 prompt，
                caller 直接 continue（不計 unclear_count——顧客明確意圖溝通）
    """
    if CANCEL_CONFIRM.run(io):
        return True
    io.speak(CANCEL_DECLINED_NOTICE)
    io.speak(reprompt)
    return False
```

兩 call site 的觸發偵測（`is_cancel_intent` vs `classify_intent l4_service ==「退出交易」`）
與成功退出動作**保留在 caller**（語意不同，硬塞會讓 helper 長 if——同 oop 傘狀 §9 精神）；
收斂的只有「NO 後尾聲」與 gate 結構。既有解釋性註解留在 call site。

## 3. 改檔範圍（高層）

| 檔 | 改動 | 行數估計 |
|---|---|---|
| `myProgram/sales/cart.py` | 新增 `classify_qty` | +18 |
| `tests/sales/test_cart.py` | 新增 5 個 classify_qty 測試（TDD 先行） | +45 |
| `myProgram/sales/states/_l2_l3_qty_followup.py` | Pass 1＋sub-loop 兩處改用 classify_qty | ±20 |
| `myProgram/sales/states/_invalid_qty_reask.py` | `_classify_into_pending` 改用 classify_qty | ±8 |
| `myProgram/sales/states/l2_l3_dialog.py` | 新增 `_cancel_gate`＋兩 call site 改用 | +14/-8 |

無檔案搬移 → 各層 code_map 條目描述不受影響（cart.py 條目「購物車（加/減/清空/結算，純函式）」
仍涵蓋分類），不需 3b 更新。

## 4. Out of scope

- `invalid_qty_reask` 主迴圈與 `_dialog_c2_second_stage` 結構（既定不入 TimedConfirm）。
- F-13（timed_call 計時包裝）→ perf_w4 評估；F-4/F-5 → perf_w2。
- 任何文案 / 計時值 / keyword / 判定優先序 / funnel 時機。
- `add_item` 既有 assert 防護與 silent-skip 語意不動。

## 5. 規範與參考

- 派 sales-coder；reuse 點：`cart.remaining_capacity`（classify_qty 內呼叫）、
  `CANCEL_CONFIRM` 單例與 `CANCEL_DECLINED_NOTICE`（l2_l3_dialog 已 import）。
- F-6 走 TDD（新函式先 RED 再 GREEN）；call site 替換屬純重構（綠→改→綠）。

## 6. 測試指令＋預期結果

1. Task 1 RED 步：`python -m pytest tests/sales/test_cart.py -q` → 5 failed（classify_qty 未存在）。
2. 每 commit 後：`python -m pytest tests/sales/` → **`509 passed`**（504 既有＋5 新增；
   既有零修改）。
3. `python -m tests.perf.bench_sales --smoke` → 7 場景全過。
4. 收尾主 agent bench 3 輪 vs perf_w1 後數據：**全項 |差| < 10%（無回歸即過）**；
   出現 >10% 變慢 → 對應 commit revert。

## 7. Commit 規範（依序）

| # | 內容 | message |
|---|---|---|
| 1 | F-6 classify_qty＋測試＋三 call site | `refactor(sales): unify qty classification via cart.classify_qty` |
| 2 | F-7 _cancel_gate＋兩 call site | `refactor(sales): extract shared cancel-gate tail for confirm substates` |

git add 明列檔名；Co-Authored-By: Claude Opus。

## 8. 流程鳥瞰

```
worktree-perf-w3（已建，基於 d7a1fd2）
  └─ spec/plan approval → commit docs → sales-coder（2 commits，F-6 內嵌 TDD）
     → 主 agent Iron Law（pytest 509＋smoke＋diff --stat＋branch）
     → spec-reviewer(sonnet) → code-quality-reviewer(opus)
     → git -C 主checkout ff-merge → push → bench 3 輪無回歸驗證
     →（cwd-pinned session）worktree/branch 清理延後新 session
```

---

## 實測結果（2026-06-12 收尾 append，Iron Law 證據）

- bench 3 輪 vs perf_w1 後數據：**全項中位數 |差| < 2%**（如 classify_intent 無命中
  7.2→7.3µs、L2 點餐劇本 23.0→22.5~23.2µs、L3 取消 11.3→11.1~11.5µs），
  遠低於 10% 閾值——冷路徑收斂零效能代價，符合 §6 預期。
- pytest `509 passed`（504 既有零修改＋5 新增 TDD 測試）；smoke 7 場景全過。
- 三段審查：spec-reviewer ✅；code-quality ⚠️ 2 Minor（測試 BDD 註解補齊
  Given/When/Then、`QtyVerdict` Literal 型別別名）→ 判決均修，`bc69969` 落地後 509 仍綠。
