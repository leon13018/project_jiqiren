# oop_w6 — 實作 plan（HOW，step-by-step）

> 對應 spec：`resources/specs/oop_w6_2026-06-10_spec.md`（§1 凍結面 / seam + §2 各節是行為權威）。
> 全 suite = `python -m pytest tests/sales/ -v --tb=short`。
> 工作目錄：worktree `.claude/worktrees/oop-w6/`，branch `worktree-oop-w6`。
> **Wave 6 招生效**。開工先 Read：現行 `tts.py` / `action.py` / `input_reader.py` / `main.py` 全文 + `test_tts_worker.py` / `test_main_read_callbacks.py` / `test_input_reader.py`（seam 契約）。

## Commit 1 — QueueWorker 基底 + 測試

1. **[RED]** 新增 `tests/sales/test_queue_worker.py`：
   - `drain_queue`：塞 3 筆 → 呼叫 → empty；空 queue 呼叫不炸
   - fake 子類別（`_process` 記錄到 list + `threading.Event` set）測骨架：
     - `submit(item)` → `_process` 收到該 item（Event wait timeout=2 同步，**禁 sleep 輪詢**）
     - `_process` raise 時 `on_done` 仍被呼叫（try/finally）——fake `_process` raise + on_done set Event
     - `on_thread_start` 先於首次 `_process`（事件序 list）
     - 多筆 submit FIFO 順序
     - `drain()` 清空未消費項
     - `thread_name` 反映在 thread（`threading.enumerate()` 找名字）
2. **[RED 證據]** 跑該檔，FAIL = `ModuleNotFoundError: No module named 'myProgram.queue_worker'`。
3. **[GREEN]** 新增 `myProgram/queue_worker.py`（spec §2-1 原文，含 ⚠️ init 順序警告 docstring；module docstring 繁中：收斂動機 + 「無 except-all / shutdown 不入基底」兩個刻意決定）。
4. 跑該檔全 PASS + 全 suite 490 不動。
5. **[COMMIT 1]**（2 檔明列）→ branch 自驗。

## Commit 2 — 兩 worker 繼承 + input_reader drain

6. `tts.py`：依 spec §2-2 改 `TtsWorker(QueueWorker)`：
   - `__init__`：**欄位先、super 後**（_q 由基底建——注意原 `__init__` 的 `self._q = queue.Queue()` 行刪除，欄位註解搬基底或保留於子類）
   - 刪 `_loop`；`_process_text` 更名 `_process`（body 一字不改）；新增 `on_done`（原 finally body）
   - `shutdown` 尾端清空迴圈 → `self.drain()`
   - class docstring 的 thread model 段保留並補「骨架在 QueueWorker」一句
7. 跑全 suite——**test_tts_worker 7 個全綠 = TtsWorker 等價證明**（R1 race / 失敗 path dec / notify / 並發 20 say / default 30s 全走新繼承路徑）。
8. `action.py`：依 spec §2-3 改 `ActionWorker(QueueWorker)`（on_thread_start / _process / do / shutdown；4 行錯誤 print 一字不改）。**自證**：回報內附舊新 `_loop`↔`on_thread_start+_process` 逐行對照表（無測試防護，靠對照）。
9. `input_reader.py`：`shutdown()` 手寫迴圈 → `drain_queue(self._q)`；`read()` **不碰**。
10. Windows import 安全自證：`python -c "import myProgram.action; import myProgram.queue_worker"`（不觸發 vendor import——worker thread 卡 q.get 不會走到 on_thread_start 的 import？**注意**：on_thread_start 在 get **之前**執行 → import action 模組建 singleton → thread 立即跑 on_thread_start → vendor import → Windows ImportError 殺 thread！**對照現行**：現行 `_loop` 頂端同樣先 import 才 get——現行 Windows 行為就是「import action 建 singleton → thread 內 vendor ImportError → daemon thread 死、主流程不受影響」（main.py finally 註解已記載此事）。**行為等價 = 保留此現狀**，本步驗證點是「import 不在主線程炸」而非「thread 不死」。
11. 全 suite 全綠。
12. **[COMMIT 2]**（3 檔明列）→ branch 自驗。

## Commit 3 — TerminalSim + 倒數 / shutdown 收斂

13. **[RED]** 新增 `tests/sales/test_terminal_sim.py`：
    - `_build_callbacks(_S1State())` 回傳鍵集 == 14 鍵集合（spec §2-5 列表）
    - `TerminalSim` 存在且 callbacks 值為 bound methods（`__self__` 同一實例）
    - 先跑：FAIL = `ImportError: cannot import name 'TerminalSim'`（_build_callbacks 鍵集測試應 PASS——它打既有 facade）
14. **[GREEN]** `main.py` 依 spec §2-5 改造：
    - `TerminalSim` 類別（13 方法 body 一字不改、`state.`→`self._state.`、lazy import 原樣）+ `callbacks()`
    - `_tick_countdown` 模組函式（spec 原文）；`read_customer_input` / `sleep` 兩方法的倒數段改用之（前後置邏輯原樣）
    - `_build_callbacks(state)` body → `return TerminalSim(state).callbacks()`（簽名 / docstring 職責保留）
    - `main()` finally 三段 → importlib 迴圈（spec §2-5）；`os._exit(0)` 段原樣
    - 歷史演進註解依 W1 政策清理（保留 stdin=DEVNULL / busy-loop hot fix / q-exit TODO 等仍成立的 why）
15. 跑全 suite——**test_main_read_callbacks 3 個全綠 = seam 證明**（fake tts 注入 + input_reader.read patch + 虛擬時鐘全走 TerminalSim 路徑）。
16. 傘狀 `resources/specs/oop_refactor_2026-06-10_design.md` §4-6 對齊（drain_queue helper / 無 on_error / shutdown 不入基底 / TerminalSim + _tick_countdown 實名）。
17. 全 suite 全綠。
18. **[COMMIT 3]**（3 檔明列）→ branch 自驗。
19. 收斂 grep：`while not (self\._q|q)\.empty\(\)` 在 `myProgram/` 應僅剩 `queue_worker.py`（drain_queue 本體）與 `input_reader.py` 的 `read()` latest-wins（語意不同保留）。

## 回報格式

- 首行 4-status；3 commit SHA + branch 驗證；兩個 RED 證據（step 2 / step 13）
- 全 suite 最終數字 + 尾 30 行
- ActionWorker 舊新逐行對照表（step 8 自證）
- step 19 收斂 grep 結果；bug 指示觸發與否
- main.py 註解清理清單（刪了哪些演進史、保留哪些 why——供 reviewer 核）
