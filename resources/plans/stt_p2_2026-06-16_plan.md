# STT Phase 2 — 讀 ch0 + 預熱連線 實作 plan

> Spec：`resources/specs/stt_p2_2026-06-16_spec.md`（WHAT）。本檔 = HOW，step-by-step TDD。
> 兩增量獨立 commit。每步一原子動作；Red→Green→Commit。所有測試 Windows 全 fake。

**目標**：STT 改讀 ReSpeaker ch0（取代 6ch 降混）+ 顧客輸入時預熱 Deepgram 連線（prewarm gate）。
**測試指令（每次驗證）**：`python -m pytest tests/stt/`（最終再加 `tests/sales/` 確認無回歸）。

---

## 增量 1 — 讀 ch0（程式內反交錯）

### Task 1.1：`_extract_ch0` 純函式

**Files**：Modify `myProgram/stt.py`（加常數 + 函式，建議置於 `_ArecordSource` 前）；Test `tests/stt/test_worker.py`

- [ ] **Step 1：寫 failing test**（append 到 `tests/stt/test_worker.py`）

```python
def test_extract_ch0_picks_first_channel():
    import struct
    from myProgram.stt import _extract_ch0
    buf = struct.pack("<6h", 11, 1, 2, 3, 4, 5) + struct.pack("<6h", 22, 6, 7, 8, 9, 0)
    assert struct.unpack("<2h", _extract_ch0(buf)) == (11, 22)


def test_extract_ch0_drops_partial_frame():
    import struct
    from myProgram.stt import _extract_ch0
    buf = struct.pack("<6h", 11, 0, 0, 0, 0, 0)
    assert _extract_ch0(buf + b"\x09\x09") == _extract_ch0(buf)
```

- [ ] **Step 2：跑見 FAIL**
Run：`python -m pytest tests/stt/test_worker.py::test_extract_ch0_picks_first_channel -v`
Expected：FAIL `ImportError: cannot import name '_extract_ch0'`

- [ ] **Step 3：寫最小實作**（`myProgram/stt.py`，置於 `_ArecordSource` class 定義前）

```python
_CHANNELS = 6  # ReSpeaker 6-channel 韌體：ch0 = 處理後（AEC/波束成形/降噪）ASR 軌


def _extract_ch0(buf: bytes, channels: int = _CHANNELS) -> bytes:
    """交錯多聲道 S16 buffer → 取 channel 0（每幀第一個 16-bit 樣本）。不完整尾幀丟棄。"""
    frame = channels * 2
    usable = len(buf) - (len(buf) % frame)
    return b"".join(buf[i:i + 2] for i in range(0, usable, frame))
```

- [ ] **Step 4：跑見 PASS**
Run：`python -m pytest tests/stt/test_worker.py -k extract_ch0 -v`
Expected：2 passed

---

### Task 1.2：`_ArecordSource.read` 反交錯讀 ch0

**Files**：Modify `myProgram/stt.py:195-196`（`_ArecordSource.read`）；Test `tests/stt/test_worker.py`

- [ ] **Step 1：寫 failing test**（append）

```python
def test_arecord_source_reads_ch0():
    import io, struct
    from myProgram.stt import _ArecordSource
    buf = b"".join(struct.pack("<6h", v, 9, 9, 9, 9, 9) for v in (1, 2, 3))

    class _P:
        stdout = io.BytesIO(buf)
        def poll(self): return None
        def terminate(self): pass

    src = _ArecordSource(_P())
    out = src.read(6)  # 要 6 bytes mono = 3 個 ch0 樣本（內部讀 36 bytes 6ch）
    assert struct.unpack("<3h", out) == (1, 2, 3)
```

- [ ] **Step 2：跑見 FAIL**
Run：`python -m pytest tests/stt/test_worker.py::test_arecord_source_reads_ch0 -v`
Expected：FAIL（現 `read` 回原始 6 bytes，非 ch0）

- [ ] **Step 3：改 `_ArecordSource.read`**（`myProgram/stt.py`）

改：
```python
    def read(self, n: int) -> bytes:
        return self._proc.stdout.read(n)
```
為：
```python
    def read(self, n: int) -> bytes:
        # 讀 n*6 bytes（6ch 交錯）→ 取 ch0（處理後）→ 回 mono；半幀尾自然順延
        return _extract_ch0(self._proc.stdout.read(n * _CHANNELS))
```

- [ ] **Step 4：跑見 PASS**
Run：`python -m pytest tests/stt/test_worker.py::test_arecord_source_reads_ch0 -v`
Expected：1 passed

---

### Task 1.3：`_default_audio_factory` 改 `-c 6` + 更新既有測試

**Files**：Modify `myProgram/stt.py:212`（factory cmd）；Modify `tests/stt/test_worker.py`（`test_default_audio_factory_command`）

- [ ] **Step 1：先改既有測試斷言**（`tests/stt/test_worker.py` `test_default_audio_factory_command` 內）

將：
```python
    assert captured["cmd"] == ["arecord", "-q", "-f", "S16_LE", "-r", "16000",
                               "-c", "1", "-t", "raw"]
```
改為：
```python
    assert captured["cmd"] == ["arecord", "-q", "-f", "S16_LE", "-r", "16000",
                               "-c", "6", "-t", "raw"]
```

- [ ] **Step 2：跑見 FAIL**
Run：`python -m pytest tests/stt/test_worker.py::test_default_audio_factory_command -v`
Expected：FAIL（prod 仍 `-c 1`）

- [ ] **Step 3：改 factory cmd**（`myProgram/stt.py` `_default_audio_factory`）

將 `"-c", "1"` 改為 `"-c", str(_CHANNELS)`：
```python
    cmd = ["arecord", "-q", "-f", "S16_LE", "-r", "16000",
           "-c", str(_CHANNELS), "-t", "raw"]
```

- [ ] **Step 4：跑全 stt 測試見 PASS**
Run：`python -m pytest tests/stt/ -v`
Expected：全 passed（含既有 + 新增 ch0 測試）

- [ ] **Step 5：commit 增量 1**

```bash
git add myProgram/stt.py tests/stt/test_worker.py
git commit -m "feat(stt): 讀 ReSpeaker ch0 處理後聲道（取代 6ch 降混）"
git branch --contains $(git rev-parse HEAD)   # 自驗落 worktree-stt-p2（Gotcha M）
```

---

## 增量 2 — 預熱連線（prewarm gate）

### Task 2.1：`SttWorker` 加 `_live` 閘 + `_start_session(live)` + `prewarm()`

**Files**：Modify `myProgram/stt.py`（`SttWorker.__init__` / `arm` / `disarm` / `_receive_loop` + 新 `_start_session` / `prewarm`）；Test `tests/stt/test_worker.py`

- [ ] **Step 1：寫 failing tests**（append）

```python
def test_prewarm_starts_session_gated():
    worker, ws, calls = _make_worker([])
    worker.prewarm()
    assert worker.is_armed()           # session 已起
    assert not worker._live.is_set()   # 閘未開（丟棄期）
    worker.arm()
    assert worker._live.is_set()       # arm 開閘 go-live
    worker.disarm()
    assert not worker._live.is_set()   # disarm 歸零


def test_prewarm_discards_received_transcript():
    worker, ws, calls = _make_worker([_results("機器人自己的話。", speech_final=True)])
    worker.prewarm()
    assert wait_until(lambda: not ws._messages)  # 訊息已被 receiver 消費
    import time; time.sleep(0.05)                 # 給 sink 被呼叫的機會（若會）
    assert calls == []                            # gated → 沒注入
    worker.disarm()
```

- [ ] **Step 2：跑見 FAIL**
Run：`python -m pytest tests/stt/test_worker.py -k prewarm -v`
Expected：FAIL `AttributeError: 'SttWorker' object has no attribute 'prewarm'`

- [ ] **Step 3：改 `SttWorker`**（`myProgram/stt.py`）

3a. `__init__` 末尾加 `_live`：
```python
        self._disabled = False    # 缺 key / 401 → 本次執行停用（鍵盤照常）
        self._live = threading.Event()  # 注入閘：set 才把辨識注入 sink（prewarm 期關）
```

3b. 將現有 `arm()` 整個方法（`def arm` 到 `sender.start()`）替換為 `_start_session(live)` + 新 `prewarm()` + 新 `arm()`：
```python
    def _start_session(self, live: bool) -> None:
        """caller 持 self._lock。起 session（ws + audio + sender/receiver threads）。
        起 thread 前依 live 先定閘——避免 receiver 早於設閘而誤丟首筆（race-safe）。
        缺 key / 連線失敗 → 停用 / 放棄（同既有）。"""
        if self._disabled or self._session is not None:
            return
        if not self._api_key:
            print("[語音辨識] ⚠️ 未設定 DEEPGRAM_API_KEY，STT 停用（鍵盤輸入照常）")
            self._disabled = True
            return
        ws = self._connect_with_retry()
        if ws is None:
            return  # 本輪放棄（已印原因）；下次 arm 再試或已永久停用
        if live:
            self._live.set()
        else:
            self._live.clear()
        audio = self._audio_factory()
        stop = threading.Event()
        receiver = threading.Thread(
            target=self._receive_loop, args=(ws, stop),
            name="SttReceiver", daemon=True)
        sender = threading.Thread(
            target=self._send_loop, args=(ws, audio, stop),
            name="SttSender", daemon=True)
        self._session = (stop, audio, ws, receiver, sender)
        receiver.start()
        sender.start()

    def prewarm(self) -> None:
        """預熱：起 session 但閘關（收到的辨識先丟棄）。冪等。"""
        with self._lock:
            self._start_session(live=False)

    def arm(self) -> None:
        """開麥 go-live：無 session 則起並開閘；已 prewarm 的 session 直接開閘。冪等。"""
        with self._lock:
            if self._session is None:
                self._start_session(live=True)
            else:
                self._live.set()
```

3c. `disarm()` 開頭 lock block 內加清閘（在 `if self._session is None` 之前）：
```python
        with self._lock:
            self._live.clear()
            if self._session is None:
                return
```

3d. `_receive_loop` 注入判斷加閘（`if text:` → `if text and self._live.is_set():`）：
```python
                if text and self._live.is_set():
                    print(f"[語音辨識] {text}")
                    self._sink(text)
```

- [ ] **Step 4：跑見 PASS（含既有 arm 路徑不破）**
Run：`python -m pytest tests/stt/test_worker.py -v`
Expected：全 passed（新 prewarm 2 測 + 既有 arm/disarm/inject/retry/401 等全綠——arm 仍起 session 並注入）

---

### Task 2.2：module-level `prewarm()` API

**Files**：Modify `myProgram/stt.py`（module 尾，`arm()` 旁）；Test `tests/stt/test_worker.py`

- [ ] **Step 1：寫 failing test**（append）

```python
def test_module_prewarm_delegates(monkeypatch):
    import myProgram.stt as stt_mod
    monkeypatch.setattr(stt_mod, "_worker", None)
    monkeypatch.setattr(stt_mod, "_default_ws_factory", lambda key: FakeWs())
    monkeypatch.setattr(stt_mod, "_default_audio_factory", lambda: FakeAudioSource())
    monkeypatch.setenv("DEEPGRAM_API_KEY", "test-key")
    stt_mod.prewarm()
    assert stt_mod._worker.is_armed() and not stt_mod._worker._live.is_set()
    stt_mod.disarm()
```

- [ ] **Step 2：跑見 FAIL**
Run：`python -m pytest tests/stt/test_worker.py::test_module_prewarm_delegates -v`
Expected：FAIL `AttributeError: module 'myProgram.stt' has no attribute 'prewarm'`

- [ ] **Step 3：加 module-level `prewarm()`**（`myProgram/stt.py`，置於 `def arm()` 上方）

```python
def prewarm() -> None:
    """對外 API：預熱連線（read_customer_input 進場呼叫，疊在 TTS 播放上）。"""
    _get_worker().prewarm()
```

- [ ] **Step 4：跑見 PASS**
Run：`python -m pytest tests/stt/test_worker.py::test_module_prewarm_delegates -v`
Expected：1 passed

---

### Task 2.3：`main.py` read_customer_input 重排 + 更新 wire-up 測試

**Files**：Modify `myProgram/main.py:143-156`（read_customer_input）；Modify `tests/stt/test_main_wireup.py`

- [ ] **Step 1：先更新 wire-up 測試**（`tests/stt/test_main_wireup.py`）

1a. `wired` fixture 的 `fake_stt` 加 `prewarm` stub：
```python
    fake_stt = types.SimpleNamespace(
        prewarm=lambda: calls.append("prewarm"),
        arm=lambda: calls.append("arm"),
        disarm=lambda: calls.append("disarm"))
```
1b. `test_arm_after_wait_idle_and_disarm_on_input` 斷言序更新：
```python
    assert (calls.index("prewarm") < calls.index("wait_idle")
            < calls.index("arm") < calls.index("read"))
    assert calls[-1] == "disarm"
```

- [ ] **Step 2：跑見 FAIL**
Run：`python -m pytest tests/stt/test_main_wireup.py -v`
Expected：FAIL（prod 尚無 prewarm 呼叫 → `ValueError: 'prewarm' is not in list`）

- [ ] **Step 3：改 `main.py read_customer_input`**

將現有（約 143–156 行）：
```python
        from myProgram import tts
        tts.wait_idle()
        from myProgram import input_reader
```
…（中略 timeout 註解）…
```python
        from myProgram import stt
        # STT Phase 1：TTS 播完才開麥（arm 冪等；缺 key 自動停用走純鍵盤）。
        # finally 保證三條路徑（拿到輸入 / timeout / 'q' sys.exit）皆收麥。
        stt.arm()
```
改為（prewarm 移到 wait_idle 前；arm 維持原位）：
```python
        from myProgram import stt
        # STT Phase 2：進場先預熱 Deepgram 連線（疊在 TTS 播放上；聽到的機器人聲
        # 因 _live 閘未開而丟棄）→ wait_idle 後 arm 開閘 go-live，省 ws 連線等待。
        stt.prewarm()
        from myProgram import tts
        tts.wait_idle()
        from myProgram import input_reader
```
…（timeout 註解段不動）…
```python
        # finally 保證三條路徑（拿到輸入 / timeout / 'q' sys.exit）皆收麥。
        stt.arm()
```

- [ ] **Step 4：跑見 PASS**
Run：`python -m pytest tests/stt/test_main_wireup.py -v`
Expected：2 passed

- [ ] **Step 5：全回歸**
Run：`python -m pytest tests/stt/ tests/sales/`
Expected：全 passed（既有 592 sales + stt 全綠）

- [ ] **Step 6：commit 增量 2**

```bash
git add myProgram/stt.py myProgram/main.py tests/stt/test_worker.py tests/stt/test_main_wireup.py
git commit -m "feat(stt): 顧客輸入預熱 Deepgram 連線（prewarm gate）"
git branch --contains $(git rev-parse HEAD)   # 自驗落 worktree-stt-p2
```

---

## 完成檢查（主 agent Iron Law）

- `python -m pytest tests/stt/ tests/sales/` → `N passed`（讀實際輸出）。
- `git branch --contains <各 commit SHA>` → `worktree-stt-p2`。
- spec §3 改檔逐條對照 `git diff --stat`。
