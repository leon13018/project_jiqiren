# oop_w1 — 實作 plan（HOW，step-by-step）

> 對應 spec：`resources/specs/oop_w1_2026-06-10_spec.md`。每 step 一原子動作；
> 測試指令一律 `python -m pytest tests/sales/ -v --tb=short`（下稱「全 suite」）。
> 工作目錄：worktree `.claude/worktrees/oop-w1/`，branch `worktree-oop-w1`。

## Commit 1 — KeywordGroup + 11 呼叫點

1. **[RED]** 新增 `tests/sales/test_keyword_group.py`，內容兩組：
   - 類別行為（直接建 `KeywordGroup(("確認取消",), ("取消",))` 之類小 fixture）：
     - substring 命中：`matches("我要確認取消了")` → True
     - substring 大小寫不敏感：`KeywordGroup(("yes cancel",)).matches("YES Cancel")` → True
     - strict-short 完全相等才命中：`matches("取消")` → True；`matches("取消會議")` → False（不含 substring 命中時）
     - strict-short 去頭尾空白 + 大小寫：`matches("  取消  ")` → True
     - 皆未命中 → False；`strict_short` 預設空 tuple 時純 substring 行為
   - 11 個 `KG_*` 實例接線（from `myProgram.sales.constants` import）：逐一 assert
     `KG_X.substrings == tuple(KEYWORDS_X)` 且 `KG_X.strict_short == tuple(KEYWORDS_X_STRICT_SHORT)`
     （11 組對照表抄 spec §2-2）；外加 2 個行為抽查：
     `KG_CANCEL_CONFIRM_NO.matches("不要取消")` → True、`KG_CONFIRM_YES.matches("沒有錯")` → True。
2. **[RED 證據]** 跑 `python -m pytest tests/sales/test_keyword_group.py -v --tb=short`，
   確認 FAIL 原因是 `ModuleNotFoundError: myProgram.sales.keyword_group` / ImportError（feature missing，非 typo）。
3. **[GREEN]** 新增 `myProgram/sales/keyword_group.py`：
   - module docstring（繁中：雙集封裝動機 — 防短詞 substring 誤命中）
   - `contains_any` / `equals_strict_short` 從 `nlu.py` **原樣搬入**（含 docstring，邏輯一字不改）
   - `KeywordGroup` frozen dataclass + `matches()`（spec §2-1 原文）
4. **[GREEN]** `myProgram/sales/nlu.py`：刪除 `contains_any` / `equals_strict_short` 本體，
   改為 `from myProgram.sales.keyword_group import contains_any, equals_strict_short`
   （re-export，置於既有 import 區；附一行繁中註明「本體已搬 keyword_group.py，re-export 保 caller 相容」）。
5. **[GREEN]** `myProgram/sales/constants/keywords.py`：
   - `from myProgram.sales.keyword_group import KeywordGroup`
   - 檔尾新增區段「KeywordGroup 配對實例（W1）」：11 個 `KG_* = KeywordGroup(tuple(KEYWORDS_X), tuple(KEYWORDS_X_STRICT_SHORT))`（對照表抄 spec §2-2）
   - `__all__` append 11 個 `KG_*` 名（既有項一個不動）
6. 跑 `tests/sales/test_keyword_group.py` 見全 PASS；跑全 suite 確認零 regression。
7. **[REFACTOR=呼叫點切換，全 suite 為守護]** 替換 11 個雙呼叫為 `.matches()`：
   - `l2_l3_dialog.py`：`_dialog_c2_second_stage` 內 CANCEL→`KG_C2_CANCEL.matches(response)`、
     CONTINUE→`KG_C2_CONTINUE.matches(response)`、CHECKOUT→`KG_C2_CHECKOUT.matches(response)`；
     `_dialog_checkout_confirm` 內 NO→`KG_CONFIRM_NO.matches(response)`（**保持在 YES 之前**）、
     YES→`KG_CONFIRM_YES.matches(response)`
   - `_cancel_confirm.py`：NO→`KG_CANCEL_CONFIRM_NO.matches(response)`（先）、YES→`KG_CANCEL_CONFIRM_YES.matches(response)`
   - `_service_confirm.py`：NO→`KG_L4_C_CONFIRM_NO.matches(response)`（先）、YES→`KG_L4_C_CONFIRM_YES.matches(response)`
   - `_invalid_qty_reask.py`：CONTINUE→`KG_INVALID_QTY_CONTINUE.matches(response)`（先）、EXIT→`KG_INVALID_QTY_EXIT.matches(response)`
   - **if 行序與周邊註解（「NO 先 check 防誤命中」等 why 註解）原位保留**
8. 各檔 import 整理（只刪真 unused）：
   - `l2_l3_dialog.py`：改 import 11→KG 名；`contains_any`/`equals_strict_short` 若已無他處使用則自 nlu import 行移除（grep 該檔確認）
   - `_cancel_confirm.py`/`_service_confirm.py`：移除 `contains_any`/`equals_strict_short` import，改 KG import
   - `_invalid_qty_reask.py`：**保留 `contains_any`**（`KEYWORDS_INVALID_QTY_CANCEL_TRIGGER` 單集呼叫仍用）、移除 `equals_strict_short`（grep 確認無他用）
   - 舊配對 list 常數 import（如 `KEYWORDS_C2_CANCEL` 等）在各 states 檔變 unused → 自 import 區移除（常數本身仍在 keywords.py 定義並 export）
9. 跑全 suite：377 + 新增全綠。
10. **[COMMIT 1]** `git add`（spec §7 第 1 條明列 8 檔）→ commit（message 範本 spec §7）→
    `git log -1 --format=%H` 記 SHA → `git branch --contains <SHA>` 確認 `worktree-oop-w1`。

## Commit 2 — parse_quantity(default=)

11. **[RED]** 新增 `tests/sales/test_parse_quantity_default.py`：
    - `parse_quantity("好的", default=None) is None`（無數字 → 回 default）
    - `parse_quantity("好的", default=1) == 1`、`parse_quantity("好的") == 1`（既有預設不變）
    - `parse_quantity("0 瓶", default=None) == 0`（顯式 0 與 default 無關）
    - `parse_quantity("三十五", default=None) == 35`（複合中文不受影響）
    - `parse_quantity("兩瓶", default=None) == 2`（單字中文映射不受影響）
12. **[RED 證據]** 跑該檔，確認 FAIL 原因 `TypeError: ... unexpected keyword argument 'default'`。
13. **[GREEN]** `nlu.py` `parse_quantity` 簽名改 `def parse_quantity(text: str, default: int | None = 1) -> int | None:`；
    函式尾 `return 1` 改 `return default`；docstring 補 default 段（規約抄 spec §2-4，含「顯式 0 與 default 無關」）。
14. 跑 `test_parse_quantity_default.py` 全 PASS + 全 suite 零 regression。
15. `product_parser.py`：
    - `parse_products` 步驟 3 呼叫點 `_parse_quantity_in_window(window)` → `parse_quantity(window, default=None)`
    - 刪 `_parse_quantity_in_window` 整個函式
    - import 改 `from myProgram.sales.nlu import parse_quantity`；移除 `_parse_compound_chinese` import
    - `CHINESE_DIGIT_MAP` 隨函式刪除變 unused → 移除
    - `KEYWORDS_ICED_TEA` / `KEYWORDS_SCRATCH`：先 `grep -rn "product_parser import" tests/ myProgram/`
      與檔內用點確認 unused → 移除；若有人經 product_parser 取用 → 保留並回報列出
    - module docstring 同步（提及借用常數的句子隨 import 現況更新）
16. 跑全 suite 全綠。
17. **[COMMIT 2]** `git add`（spec §7 第 2 條明列 3 檔）→ commit → branch 自驗。

## Commit 3 — L4 exit 合併 + 傘狀 map 修正

18. `l4.py`：新增 `_l4_exit_to_l1(speak, cart, notice: str)`（spec §2-5 原文）；
    `run_l4` budget 耗盡點改 `_l4_exit_to_l1(speak, cart, L4_D_FORCED_EXIT)`；
    `_l4_dispatch_response` 拒絕 YES 點改 `_l4_exit_to_l1(speak, cart, L4_B_CANCEL_THANKS)`；
    刪 `_l4_exit_b` / `_l4_exit_d_forced`。
    （無新測試：兩路徑已被既有 run_l4 行為測試覆蓋 — test_states L4 區段；本步是等價收斂，全 suite 即守護。）
19. 跑全 suite 全綠。
20. `resources/specs/oop_refactor_2026-06-10_design.md`：§3 類別地圖
    `constants/keywords.py KeywordGroup + 組合實例` 行改為兩行（`keyword_group.py（新）— KeywordGroup 類別 + 比對原語（W1）` / `constants/keywords.py — KG_* 組合實例（W1；既有 list 常數全保留）`）；
    §4-1 標題補檔位說明（類別在 keyword_group.py、實例在 keywords.py、nlu re-export）。
21. **[COMMIT 3]** `git add`（spec §7 第 3 條明列 2 檔）→ commit → branch 自驗。

## 回報格式

- 首行 4-status（DONE / DONE_WITH_CONCERNS / BLOCKED / NEEDS_CONTEXT）
- 3 個 commit SHA + `git branch --contains` 結果
- RED 證據摘要（step 2 / step 12 的 FAIL 原因行）
- 全 suite 最終數字（`N passed`）+ 輸出尾 30 行
- KEYWORDS_ICED_TEA / KEYWORDS_SCRATCH 處置結果（移除 or 保留+原因）
