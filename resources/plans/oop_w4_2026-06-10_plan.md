# oop_w4 — 實作 plan（HOW，step-by-step）

> 對應 spec：`resources/specs/oop_w4_2026-06-10_spec.md`（§2-5 矩陣 + §2-6 quirk 是行為權威）。
> 全 suite = `python -m pytest tests/sales/ -v --tb=short`。
> 工作目錄：worktree `.claude/worktrees/oop-w4/`，branch `worktree-oop-w4`。
> **Wave 6 招生效**；追加：每完成一個 session 方法跑一次全 suite。任何紅 → 對照 §2-5 矩陣找行為差異，**禁改測試**；矩陣與現碼衝突 → 以現碼為準並停下回報。

## Commit 1 — ModePolicy + DialogSession 核心

1. **[RED]** 新增 `tests/sales/test_mode_policy.py`：
   - `from myProgram.sales.states.l2_l3_dialog import ModePolicy, L2_POLICY, L3_POLICY, DialogSession`
   - 兩 policy 全欄位接線 assert（§2-1 表 9 個資料欄逐一；含 `think_limit` 3/4、`silence_think_writeback` False/True）
   - `isinstance(L2_POLICY, ModePolicy)` / `isinstance(L3_POLICY, ModePolicy)`
   - `session.policy()` 推導：空 cart → `is L2_POLICY`；對同一 session 的 cart `add_item` 後再呼叫 → `is L3_POLICY`（證明不快取）
   - 行為抽查：`DialogSession(io, {}, 0).main_loop()`（scripted io：read 回 None）→ 回傳 `("L1_via_subroutine_a", 0)` 且 speak 收到 `L2_TIMEOUT_TO_HAWK_VOICE`
2. **[RED 證據]** 跑該檔，FAIL = `ImportError: cannot import name 'ModePolicy' (或 L2_POLICY)`。
3. **[GREEN-1]** `l2_l3_dialog.py` 加 `ModePolicy(ABC)` + `L2Policy` / `L3Policy`（資料欄 + 5 hooks，§2-1 表逐格）+ `L2_POLICY` / `L3_POLICY` 單例。hooks 此階段引用的 session 方法尚未存在——hooks 本體照 §2-1 表寫死（晚綁定，呼叫時才解析）。
4. **[GREEN-2]** 加 `DialogSession.__init__` / `policy()`。跑 `test_mode_policy.py` 的接線測試轉綠（行為抽查仍紅，等 main_loop）。
5. 實作 session 方法（每完成一個跑全 suite）：
   a. `exit_a()`——委派既有 `_dialog_exit_a(self.io, self.cart)`（C2 才內聯）
   b. `_reenter_speak(control)`——prefix 三元 + 當前 cart 即時判定 entry（§2-3 注意 (c)）
   c. `checkout_flow()`——§2-2 定義（confirm → yes/cancel_to_l1/其他 三路）
   d. `_dispatch_inner(response)`——§2-4 流程逐分支（Q2/Q3：無任何 unclear / think-reset 操作）
   e. `_think_silence()`——§2-4（reask 用 `self.policy().reask`）
   f. `main_loop()`——§2-3 流程逐分支（入口 `self.unclear_count = 0`；想一下分支含 Q1 snapshot/restore + B11；商品分支含 was_empty/B11）
6. 切換接線 + 刪舊：
   - `run_dialog` 末行 → `return DialogSession(io, cart, think_count).main_loop()`
   - `_dialog_c2_second_stage` / `_c2_checkout_via_confirm`（仍為模組函式）內的 `_dialog_main_loop(io=..., cart=..., think_count=...)` 呼叫 → `DialogSession(io, cart, think_count).main_loop()`（fresh 重入；main_loop 入口 unclear=0 與原函式 local 0 等價）
   - policy hooks 內 c2 呼叫 → 模組函式 `_dialog_c2_second_stage(io=session.io, cart=session.cart, think_count=session.think_count)`
   - 刪 `_dialog_main_loop` / `_dialog_think_silence_l2` / `_dialog_think_silence_l3` / `_dialog_dispatch_inner_l2` / `_dialog_dispatch_inner_l3`（5 函式；`_dialog_exit_a` C1 保留）
   - 既有行為性 why 註解（B11 / 合成 voice / C13 / INTENTIONAL 無 wall-clock…）搬到對應新位置，不丟
7. 全 suite：469 + 新測試全綠。
8. **[COMMIT 1]**（`myProgram/sales/states/l2_l3_dialog.py`、`tests/sales/test_mode_policy.py` 明列）→ branch 自驗。

## Commit 2 — c2 家族方法化 + docs

9. `_dialog_c2_second_stage` → `DialogSession.c2_second_stage()`、`_c2_checkout_via_confirm` → `DialogSession._c2_checkout_via_confirm()`：邏輯**逐行原樣**（docstring 一併搬），引用改 `self.io / self.cart / self.think_count`；CONTINUE 重入 → speak `L3_C2_CONTINUE_ACK` 後 `return self.main_loop()`（self 帶 think_count = 現行傳參等價；main_loop 入口 unclear=0 = 現行 fresh local 等價）；CHECKOUT / 倒數歸零 / silent → `self._c2_checkout_via_confirm()`。刪兩個模組函式；policy hooks 的 c2 呼叫改 `session.c2_second_stage()`。
10. `_dialog_exit_a` 內聯為 `exit_a()` 方法本體；刪模組函式；c2 方法內呼叫改 `self.exit_a()`。
11. module docstring 更新：補一段「W4：ModePolicy（Strategy）+ DialogSession 持 io/cart/計數器；mode 每輪由 cart 推導」；刪已不存在函式名的引用（檔頭 callback 集合段的 `_dialog_dispatch_inner_l2 / _dialog_main_loop` 字樣改為現名）。
12. 傘狀 `resources/specs/oop_refactor_2026-06-10_design.md` §4-4：hook 清單對齊實作（5 hooks + `silence_think_writeback`；補一句「checkout 拆 main/inner 兩 hook——沉默期語境不碰 unclear 的既有不對稱」）。
13. 全 suite 全綠；grep 確認 `_dialog_main_loop|_dialog_think_silence|_dialog_dispatch_inner|_dialog_exit_a` 在 myProgram/ 0 殘留（docstring 內歷史敘述除外，一併更新）。
14. **[COMMIT 2]**（`myProgram/sales/states/l2_l3_dialog.py`、`resources/specs/oop_refactor_2026-06-10_design.md` 明列）→ branch 自驗。

## 回報格式

- 首行 4-status；2 commit SHA + branch 驗證；RED 證據（step 2）
- 全 suite 最終數字 + 尾 30 行
- §2-6 三 quirk 的保全自證（各指出實作中對應行）
- step 13 收斂 grep 結果
- 若觸發「發現 bug 直接修」常設指示 → 獨立列出（發現什麼 / 為何是 bug / 怎麼修 / 哪個 commit）
