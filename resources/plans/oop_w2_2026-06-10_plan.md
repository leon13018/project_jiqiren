# oop_w2 — 實作 plan（HOW，step-by-step）

> 對應 spec：`resources/specs/oop_w2_2026-06-10_spec.md`。
> 全 suite = `python -m pytest tests/sales/ -v --tb=short`。
> 工作目錄：worktree `.claude/worktrees/oop-w2/`，branch `worktree-oop-w2`。
> **Wave 6 招生效**：開工先列受影響函式清單；每改完一檔跑全 suite；commit 前 `git diff --stat` + `git status` + 全 suite verbose；任何 fail 不 commit。

## Commit 1 — DialogIO + 傘狀修正

1. **[RED]** 新增 `tests/sales/test_dialog_io.py`：
   - `speak_blocking` 有 `speak_and_wait` → 呼叫它、`speak` 不被呼叫（兩個 list-append lambda 記錄）
   - `speak_blocking` 無 `speak_and_wait`（None）→ fallback 呼叫 `speak`
   - 欄位注入保持：建構後 `io.speak is f`、`io.read_customer_input is g`
   - `print_terminal` / `do_action` / `speak_and_wait` 預設 None
   - frozen：對 `io.speak` 賦值 raise `dataclasses.FrozenInstanceError`
2. **[RED 證據]** 跑該檔，確認 FAIL = `ModuleNotFoundError: No module named 'myProgram.sales.dialog_io'`。
3. **[GREEN]** 新增 `myProgram/sales/dialog_io.py`（spec §2-1 原文，含 docstring）。跑該檔全 PASS + 全 suite 444 不動。
4. 修傘狀 `resources/specs/oop_refactor_2026-06-10_design.md` §4-2：`print_terminal` / `do_action` 加 `= None` 預設 + 一行部分注入理由。
5. **[COMMIT 1]**（spec §7 第 1 條，3 檔明列）→ branch 自驗。

## Commit 2 — l2_l3_dialog 貫通（最大宗，逐函式做）

6. 開工先列：grep 該檔全部 `def _` 簽名與互相呼叫點，thinking 內列轉換清單（spec §2-2 表為準）。
7. `run_dialog` facade：建 `io = DialogIO(...)`（spec §2-3）；entry 的 `opencv_disable()` 原位；`do_action(...)` / `speak(...)` 改 `io.do_action(...)` / `io.speak(...)`；呼叫 `_dialog_main_loop(io=io, cart=cart, think_count=think_count)`。
8. 逐函式轉換（每完成 2-3 個函式跑一次全 suite，fail 立即停）：
   - `_dialog_main_loop(io, cart, think_count)`：體內 `speak`→`io.speak`、`read_customer_input`→`io.read_customer_input`、`do_action`→`io.do_action`；下傳全改 `io`；`cancel_confirm(io.speak, io.read_customer_input, speak_and_wait=io.speak_and_wait)`；`service_confirm(speak=io.speak, print_terminal=io.print_terminal, read_customer_input=io.read_customer_input, speak_and_wait=io.speak_and_wait, allow_scan=False)`；`resolve_and_add_products(products=products, cart=cart, speak=io.speak, print_terminal=io.print_terminal, read_customer_input=io.read_customer_input, classify_intent_mode=nlu_mode, speak_and_wait=io.speak_and_wait)`
   - `_dialog_exit_a(io, cart)`、`_handle_checkout_confirm_result(result, cart, io)`
   - `_dialog_think_silence_l2/_l3(io, cart, think_count)`
   - `_dialog_dispatch_inner_l2/_l3(response, io, cart, think_count)`
   - `_dialog_checkout_confirm(io, cart)`、`_dialog_unclear_final_confirmation(io, cart)`
   - `_dialog_c2_second_stage(io, cart, think_count)`：刪 `_speak_blocking` 行 → `io.speak_blocking(L3_C2_WARNING_TEMPLATE...)`
   - `_c2_checkout_via_confirm(io, cart, think_count)`
9. 函式 docstring 內 Args 段同步（刪已併入 io 的逐項 callback 說明，補一行 `io: DialogIO`；**行為性 why 註解原位保留**）。
10. 全 suite 全綠（444 + C1 新增）。
11. **[COMMIT 2]**（單檔明列）→ branch 自驗。

## Commit 3 — l4 貫通

12. `run_l4` facade：建 io；`opencv_disable()` 原位；`_speak_blocking` 行刪 → `io.speak_blocking(L4_ENTRY_PROMPT_TEMPLATE...)`；reset 分支的 re-speak 同步用 io。
13. 轉換：`_l4_print_entry_detail(cart, total, io)`（體內 `print_terminal`→`io.print_terminal`）、`_l4_exit_to_l1(io, cart, notice)`、`_l4_dispatch_response(response, io, cart)`（`cancel_confirm` / `service_confirm` 呼叫自 io 展開）、`_l4_service_mode(io, cart)`。
14. 全 suite 全綠。
15. **[COMMIT 3]** → branch 自驗。

## Commit 4 — qty followup + confirm 家族內部

16. `_cancel_confirm.py` `cancel_confirm`：簽名不動；體內 `io = DialogIO(speak=speak, read_customer_input=read_customer_input, speak_and_wait=speak_and_wait)`；刪 `_speak_blocking` 行 → `io.speak_blocking(CANCEL_CONFIRM_PROMPT)`；`read_customer_input(timeout=remaining)` → `io.read_customer_input(...)`。
17. `_service_confirm.py` `service_confirm`：同 pattern（多 `print_terminal`）；`io.print_terminal(SERVICE_PHONE)`。
18. `_invalid_qty_reask.py`：`invalid_qty_cancel_confirm` 與 `invalid_qty_reask` 各自體內建 io、刪 2 處 `_speak_blocking` 行；`invalid_qty_reask` 內 `service_confirm(...)` 呼叫自 io 展開。
19. `_l2_l3_qty_followup.py`：`resolve_and_add_products` 簽名不動、體內建 io、刪 `_speak_blocking` 行；Pass1 `speak(...)` → `io.speak(...)`；`invalid_qty_reask(invalid_pending, cart, io.speak, io.print_terminal, io.read_customer_input, io.speak_and_wait)`；`_qty_follow_up_sub_loop(product, unit, io, classify_intent_mode, cart)` 簽名轉換，體內同步（`service_confirm` / `invalid_qty_reask` 凍結呼叫自 io 展開）。
20. 全 repo grep `_speak_blocking` 應為 **0 命中**（8 處全清）。
21. 全 suite 全綠。
22. **[COMMIT 4]**（4 檔明列）→ branch 自驗。

## 回報格式

- 首行 4-status；4 commit SHA + branch 驗證；RED 證據（step 2）
- 全 suite 最終數字 + 尾 30 行；`_speak_blocking` grep 0 命中確認
- 受影響函式清單（Wave 6 招第 1 招的產出）
