# STT Phase 2 v2 — 讀 ch0 + 來源端閘 prewarm 實作 plan

> Spec：`resources/specs/stt_p2v2_2026-06-16_spec.md`（WHAT）。本檔 = HOW，step-by-step TDD。
> 兩增量獨立 commit。每步一原子動作；Red→Green→Commit。Windows 全 fake。
> 基線 = 已 revert 的 Phase 1 stt.py（`arecord -c 1`、tuple session、無 prewarm/keepalive/gate）。

**測試指令**：`python -m pytest tests/stt/`（最終加 `tests/sales/`）。

---

## 增量 1 — 讀 ch0（程式內反交錯）

### Task 1.1：`_extract_ch0` 純函式

**Files**：Modify `myProgram/stt.py`（`_ArecordSource` 前加常數+函式）；Test `tests/stt/test_worker.py`

- [ ] **Step 1：failing test**（append `tests/stt/test_worker.py`）
```python
def test_extract_ch0_picks_first_channel():
    import struct
    from myProgram.stt import _extract_ch0
    buf = struct.pack("<6h", 11, 1, 2, 3, 4, 5) + struct.pack("<6h", 22, 6, 7, 8, 9, 0)
    assert struct.unpack("<2h", _extract_ch0(buf)) == (11, 22)


def test_extract_ch0_drops_partial_frame():
    import struct
    from myProgram.stt import _extract_ch0
    buf = struct.pack("<6h", 11, 0, 0, 0, 0, 0)
    assert _extract_ch0(buf + b"\x09\x09") == _extract_ch0(buf)
```
- [ ] **Step 2：FAIL** `python -m pytest tests/stt/test_worker.py -k extract_ch0 -v` → ImportError
- [ ] **Step 3：實作**（`myProgram/stt.py`，`_ArecordSource` class 前）
```python
_CHANNELS = 6  # ReSpeaker 6-channel 韌體：ch0 = 處理後（AEC/波束成形/降噪）ASR 軌


def _extract_ch0(buf: bytes, channels: int = _CHANNELS) -> bytes:
    """交錯多聲道 S16 buffer → 取 channel 0（每幀第一個 16-bit 樣本）。不完整尾幀丟棄。"""
    frame = channels * 2
    usable = len(buf) - (len(buf) % frame)
    return b"".join(buf[i:i + 2] for i in range(0, usable, frame))
```
- [ ] **Step 4：PASS** `python -m pytest tests/stt/test_worker.py -k extract_ch0 -v` → 2 passed

### Task 1.2：`_ArecordSource.read` 反交錯

**Files**：Modify `myProgram/stt.py`（`_ArecordSource.read`）；Test `tests/stt/test_worker.py`

- [ ] **Step 1：failing test**（append）
```python
def test_arecord_source_reads_ch0():
    import io, struct
    from myProgram.stt import _ArecordSource
    buf = b"".join(struct.pack("<6h", v, 9, 9, 9, 9, 9) for v in (1, 2, 3))

    class _P:
        stdout = io.BytesIO(buf)
        def poll(self): return None
        def terminate(self): pass

    out = _ArecordSource(_P()).read(6)  # 6 bytes mono = 3 個 ch0 樣本（內部讀 36 bytes）
    assert struct.unpack("<3h", out) == (1, 2, 3)
```
- [ ] **Step 2：FAIL**（現 read 回原始 bytes）
- [ ] **Step 3：改 `read`**
將 `return self._proc.stdout.read(n)` 改為：
```python
        # 讀 n*6 bytes（6ch 交錯）→ 取 ch0（處理後）→ 回 mono
        return _extract_ch0(self._proc.stdout.read(n * _CHANNELS))
```
- [ ] **Step 4：PASS** `python -m pytest tests/stt/test_worker.py::test_arecord_source_reads_ch0 -v`

### Task 1.3：factory `-c 6` + 更新既有測試

**Files**：Modify `myProgram/stt.py`（`_default_audio_factory`）；Modify `tests/stt/test_worker.py`（`test_default_audio_factory_command`）

- [ ] **Step 1：改既有測試斷言**：`test_default_audio_factory_command` 內 `"-c", "1"` → `"-c", "6"`
- [ ] **Step 2：FAIL**（prod 仍 `-c 1`）
- [ ] **Step 3：改 factory**：`cmd` 內 `"-c", "1"` → `"-c", str(_CHANNELS)`
- [ ] **Step 4：PASS 全 stt** `python -m pytest tests/stt/ -v`
- [ ] **Step 5：commit 增量 1**
```bash
git add myProgram/stt.py tests/stt/test_worker.py
git commit -m "feat(stt): 讀 ReSpeaker ch0 處理後聲道（取代 6ch 降混）"
git branch --contains $(git rev-parse HEAD)
```

---

## 增量 2 v2 — 來源端閘 prewarm（修正自我回授）

### Task 2.1：SttWorker 重構（dict session + `_open_ws`/`prewarm`/`arm` split + `_keepalive_loop` + `disarm` dict）

**Files**：Modify `myProgram/stt.py`（`SttWorker.__init__` / `arm` / `disarm` + 新 `_open_ws`/`prewarm`/`_keepalive_loop`）；Test `tests/stt/test_worker.py`

- [ ] **Step 1：failing tests**（append）
```python
def test_prewarm_only_keepalive_no_audio():
    ws = FakeWs([])
    worker = SttWorker(sink=lambda t: None, api_key="test-key",
                       ws_factory=lambda key: ws, audio_factory=FakeAudioSource,
                       keepalive_interval=0.02)
    worker.prewarm()
    assert worker.is_armed()                                   # session 已起
    assert wait_until(lambda: any(isinstance(m, str) and "KeepAlive" in m
                                  for m in ws.sent))           # 有送 KeepAlive
    assert all(isinstance(m, str) for m in ws.sent)            # 全是 KeepAlive，零音訊 bytes
    worker.disarm()


def test_arm_sends_audio_after_prewarm():
    ws = FakeWs([_results("我要紅茶兩杯。", speech_final=True)])
    calls = []
    worker = SttWorker(sink=calls.append, api_key="test-key",
                       ws_factory=lambda key: ws,
                       audio_factory=lambda: FakeAudioSource([b"\x01\x02"]),
                       keepalive_interval=5.0)
    worker.prewarm()
    worker.arm()                                               # 開始送音訊
    assert wait_until(lambda: b"\x01\x02" in ws.sent)          # 音訊送出
    assert wait_until(lambda: calls == ["我要紅茶兩杯"])       # 顧客辨識注入
    worker.disarm()
```
- [ ] **Step 2：FAIL** `python -m pytest tests/stt/test_worker.py -k "prewarm or arm_sends" -v` → AttributeError prewarm / keepalive_interval

- [ ] **Step 3：重構 `SttWorker`**（`myProgram/stt.py`）

3a. `__init__` 改（加 `keepalive_interval` 參數 + session 改 dict 註解）：
```python
    def __init__(self, sink, api_key=None, audio_factory=None, ws_factory=None,
                 keepalive_interval=5.0):
        self._sink = sink
        self._api_key = api_key
        self._audio_factory = audio_factory or _default_audio_factory
        self._ws_factory = ws_factory or _default_ws_factory
        self._keepalive_interval = keepalive_interval  # prewarm 期維持連線間隔（秒）
        self._lock = threading.Lock()
        self._session = None      # dict|None: stop/ws/receiver/keepalive/sending/audio/sender
        self._disabled = False    # 缺 key / 401 → 本次執行停用（鍵盤照常）
```

3b. 將現有 `arm()` 整段（`def arm` 到 `sender.start()`）替換為 `_open_ws` + `prewarm` + `arm` + `_keepalive_loop`：
```python
    def _open_ws(self) -> bool:
        """caller 持 self._lock。連 ws + 起 receiver + keepalive（不送音訊）。
        缺 key / 連線失敗 → 停用 / 放棄。回傳 session 是否就緒。"""
        if self._disabled:
            return False
        if self._session is not None:
            return True
        if not self._api_key:
            print("[語音辨識] ⚠️ 未設定 DEEPGRAM_API_KEY，STT 停用（鍵盤輸入照常）")
            self._disabled = True
            return False
        ws = self._connect_with_retry()
        if ws is None:
            return False  # 本輪放棄（已印原因）
        stop = threading.Event()
        sending = threading.Event()  # set 後 keepalive 停（audio 接手維持連線）
        receiver = threading.Thread(
            target=self._receive_loop, args=(ws, stop),
            name="SttReceiver", daemon=True)
        keepalive = threading.Thread(
            target=self._keepalive_loop, args=(ws, stop, sending),
            name="SttKeepAlive", daemon=True)
        self._session = {"stop": stop, "ws": ws, "receiver": receiver,
                         "keepalive": keepalive, "sending": sending,
                         "audio": None, "sender": None}
        receiver.start()
        keepalive.start()
        return True

    def prewarm(self) -> None:
        """預熱：連 ws + 起 receiver/keepalive，不送音訊（機器人聲不進 Deepgram）。冪等。"""
        with self._lock:
            self._open_ws()

    def arm(self) -> None:
        """開始送顧客音訊：確保連線 → 停 keepalive + 起 arecord/sender。冪等。"""
        with self._lock:
            if not self._open_ws():
                return
            s = self._session
            if s["sender"] is not None:
                return  # 已 armed
            s["sending"].set()  # 通知 keepalive 停送（audio 接手維持連線）
            audio = self._audio_factory()
            sender = threading.Thread(
                target=self._send_loop, args=(s["ws"], audio, s["stop"]),
                name="SttSender", daemon=True)
            s["audio"] = audio
            s["sender"] = sender
            sender.start()

    def _keepalive_loop(self, ws, stop, sending) -> None:
        """prewarm 期週期送 KeepAlive（text frame）維持 Deepgram 連線；
        送音訊（sending set）或 stop 後即止。"""
        while not stop.wait(self._keepalive_interval):
            if sending.is_set():
                return
            try:
                ws.send(json.dumps({"type": "KeepAlive"}))
            except Exception:
                return
```
（`_connect_with_retry` / `_send_loop` / `_receive_loop` **不動**。）

3c. 將 `disarm()` 整段（tuple 解包版）替換為 dict 版：
```python
    def disarm(self) -> None:
        """冪等收麥：stop → 殺音源(若有) + 關 ws → join receiver/keepalive/sender。"""
        with self._lock:
            if self._session is None:
                return
            s = self._session
            self._session = None
            s["stop"].set()
            if s["audio"] is not None:
                s["audio"].close()
            try:
                s["ws"].close()
            except Exception:
                pass  # 已斷線的 ws close 可能 raise——cleanup 路徑安全吞掉
        for th in (s["receiver"], s["keepalive"], s["sender"]):
            if th is not None:
                th.join(timeout=1.0)
```

- [ ] **Step 4：PASS（含既有 arm/disarm/inject/retry/401 路徑不破）** `python -m pytest tests/stt/test_worker.py -v`

### Task 2.2：module-level `prewarm()`

**Files**：Modify `myProgram/stt.py`（module 尾，`def arm()` 上方）；Test `tests/stt/test_worker.py`

- [ ] **Step 1：failing test**（append）
```python
def test_module_prewarm_delegates(monkeypatch):
    import myProgram.stt as stt_mod
    monkeypatch.setattr(stt_mod, "_worker", None)
    monkeypatch.setattr(stt_mod, "_default_ws_factory", lambda key: FakeWs())
    monkeypatch.setattr(stt_mod, "_default_audio_factory", lambda: FakeAudioSource())
    monkeypatch.setenv("DEEPGRAM_API_KEY", "test-key")
    stt_mod.prewarm()
    assert stt_mod._worker.is_armed()
    stt_mod.disarm()
```
- [ ] **Step 2：FAIL**（no module prewarm）
- [ ] **Step 3：加**（`myProgram/stt.py`，`def arm()` 上方）
```python
def prewarm() -> None:
    """對外 API：預熱連線（read_customer_input 進場呼叫，疊在 TTS 播放上）。"""
    _get_worker().prewarm()
```
- [ ] **Step 4：PASS**

### Task 2.3：`main.py` wiring + 更新 wire-up 測試

**Files**：Modify `myProgram/main.py`（`read_customer_input`）；Modify `tests/stt/test_main_wireup.py`

- [ ] **Step 1：更新 wire-up 測試**
1a. `wired` fixture 的 `fake_stt` 加 `prewarm`：
```python
    fake_stt = types.SimpleNamespace(
        prewarm=lambda: calls.append("prewarm"),
        arm=lambda: calls.append("arm"),
        disarm=lambda: calls.append("disarm"))
```
1b. `test_arm_after_wait_idle_and_disarm_on_input` 斷言序：
```python
    assert (calls.index("prewarm") < calls.index("wait_idle")
            < calls.index("arm") < calls.index("read"))
    assert calls[-1] == "disarm"
```
- [ ] **Step 2：FAIL**（prod 無 prewarm 呼叫）
- [ ] **Step 3：改 `main.py read_customer_input`**
將現有（`read_customer_input` 內）：
```python
        from myProgram import tts
        tts.wait_idle()
        from myProgram import input_reader
```
改為（prewarm 移到 wait_idle 前）：
```python
        from myProgram import stt
        # STT Phase 2 v2：進場先預熱 Deepgram 連線（只連線、不送音訊；播放期送 KeepAlive
        # 維持）→ wait_idle 後 arm 才開始送顧客音訊，省 ws 連線等待、且機器人聲不進辨識。
        stt.prewarm()
        from myProgram import tts
        tts.wait_idle()
        from myProgram import input_reader
```
並把原本 `stt.arm()` 上方的 `from myProgram import stt` 註解更新（已提前 import，移除重複行）：原
```python
        from myProgram import stt
        # STT Phase 1：TTS 播完才開麥（arm 冪等；缺 key 自動停用走純鍵盤）。
        # finally 保證三條路徑（拿到輸入 / timeout / 'q' sys.exit）皆收麥。
        stt.arm()
```
改為（移除重複 import + 更新註解）：
```python
        # STT Phase 2 v2：TTS 播完才 arm（開始送音訊 go-live；連線已預熱，零等待）。
        # finally 保證三條路徑（拿到輸入 / timeout / 'q' sys.exit）皆收麥。
        stt.arm()
```
- [ ] **Step 4：PASS** `python -m pytest tests/stt/test_main_wireup.py -v`
- [ ] **Step 5：全回歸** `python -m pytest tests/stt/ tests/sales/`（既有 592 + stt 全綠）
- [ ] **Step 6：commit 增量 2**
```bash
git add myProgram/stt.py myProgram/main.py tests/stt/test_worker.py tests/stt/test_main_wireup.py
git commit -m "feat(stt): 來源端閘 prewarm 預熱連線（修正 v1 自我回授）"
git branch --contains $(git rev-parse HEAD)
```

---

## 完成檢查（主 agent Iron Law）
- `python -m pytest tests/stt/ tests/sales/` → `N passed`。
- `git branch --contains <各 SHA>` → `worktree-stt-p2v2`。
- spec §3 改檔逐條對照 `git diff --stat`。
