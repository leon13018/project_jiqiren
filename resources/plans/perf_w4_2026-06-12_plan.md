# perf_w4 實作計畫（plan — HOW）

> **執行者**：sales-coder。對應 spec：`resources/specs/perf_w4_2026-06-12_spec.md`。
> 純重構規約：綠 → 改 → 綠 → commit；每 task 一 commit；任一步 pytest 不是 `509 passed`
> 即停下回報。每 commit 後 `git branch --contains HEAD` 自驗。

**Goal**：F-8 entry-prompt helper＋F-12 死常數連衛兵移除，行為零改變、測試函式零增刪。

---

## Task 0：基線驗證

- [ ] **Step 0.1**：`python -m pytest tests/sales/` → 預期 `509 passed`。

---

## Task 1（F-8）：`_entry_prompt_for` helper

**Files**：Modify `myProgram/sales/states/l2_l3_dialog.py`

- [ ] **Step 1.1**：`run_dialog` 定義之前插入：

```python
def _entry_prompt_for(cart) -> str:
    """cart 世界狀態 → entry prompt（空 → L2、非空 → L3）；進場與 reenter 共用。"""
    return L2_ENTRY_PROMPT if cart_module.is_empty(cart) else L3_ENTRY_PROMPT
```

- [ ] **Step 1.2**：run_dialog 進場替換（`:149` 的 ACTION 三元式與前後註解不動）

舊：

```python
    # Entry prompt 按 cart 狀態決定
    io.speak(L2_ENTRY_PROMPT if cart_module.is_empty(cart) else L3_ENTRY_PROMPT)
```

新：

```python
    # Entry prompt 按 cart 狀態決定
    io.speak(_entry_prompt_for(cart))
```

- [ ] **Step 1.3**：reenter 替換

舊：

```python
        entry = L2_ENTRY_PROMPT if cart_module.is_empty(self.cart) else L3_ENTRY_PROMPT
        self.io.speak(prefix + entry)
```

新：

```python
        entry = _entry_prompt_for(self.cart)
        self.io.speak(prefix + entry)
```

- [ ] **Step 1.4**：`python -m pytest tests/sales/` → `509 passed`
- [ ] **Step 1.5**：

```bash
git add myProgram/sales/states/l2_l3_dialog.py
git commit -m "refactor(sales): extract _entry_prompt_for shared by dialog entry and reenter"
git branch --contains HEAD
```

---

## Task 2（F-12）：死常數連衛兵移除

**Files**：Modify `myProgram/sales/constants/timing.py`、`tests/sales/test_constants.py`、
`tests/sales/test_states.py`

- [ ] **Step 2.1**：`timing.py` 刪常數段

舊（整段刪除）：

```python
# L3 鏈路 C-2「請問是否要結帳？」嚴格 yes/no 子狀態的 wall-clock 倒數秒數
# （2026-05-26 從 10 → 12：跟 DnC/DyC/checkout-confirm 對齊；同時改嚴格 yes/no
#   倒數不重置 — 亂答忽略不重置計時器，只認 CONFIRM_YES / CONFIRM_NO + 終端 1/2）
AUTO_CHECKOUT_NOTICE: int = 12

```

- [ ] **Step 2.2**：`timing.py` `C2_DECISION_TIMEOUT` 註解首行

舊：`# L3 C-2 第二段三選一倒數秒數（2026-05-28 加；新設計從 AUTO_CHECKOUT_NOTICE=12 縮短為 6）`
新：`# L3 C-2 第二段三選一倒數秒數（2026-05-28 加；自原 C-2 嚴格 yes/no 12s 設計縮短為 6）`

- [ ] **Step 2.3**：`timing.py` `__all__` 刪 `    "AUTO_CHECKOUT_NOTICE",` 一行；
  模組 docstring（:5）`THANK_DELAY / AUTO_CHECKOUT_NOTICE / UNCLEAR_MAX` 改
  `THANK_DELAY / UNCLEAR_MAX`。

- [ ] **Step 2.4**：`test_constants.py` scenario 註解與 assert

舊：

```python
### Then WAIT_NO_RESPONSE=6 / HAWK_INTERVAL=12 / OPENCV_MUTE=6 /
###      THANK_DELAY=3 / AUTO_CHECKOUT_NOTICE=12 / OPENCV_DWELL=1.5
###      （AUTO_CHECKOUT_NOTICE 2026-05-26 從 10 → 12；OPENCV_MUTE 同日從 12 → 6，
###       12s 對展演節奏太久，6s 已足夠擋掉「同一顧客剛走又走回」）
```

新：

```python
### Then WAIT_NO_RESPONSE=6 / HAWK_INTERVAL=12 / OPENCV_MUTE=6 /
###      THANK_DELAY=3 / OPENCV_DWELL=1.5
###      （OPENCV_MUTE 2026-05-26 從 12 → 6，12s 對展演節奏太久，
###       6s 已足夠擋掉「同一顧客剛走又走回」）
```

並刪 `    assert const.AUTO_CHECKOUT_NOTICE == 12` 一行（測試函式本身保留）。

- [ ] **Step 2.5**：`test_states.py` 刪 import 清單中 `    AUTO_CHECKOUT_NOTICE,` 一行
  （:29，未使用）；過時 scenario 註解修正：

舊：`### Then 系統 speak C-2 第一段語音，進入第二段等待 AUTO_CHECKOUT_NOTICE（10）秒`
新：`### Then 系統 speak C-2 第一段語音，進入第二段等待 C2_DECISION_TIMEOUT（6）秒`

- [ ] **Step 2.6**：grep 自驗：`AUTO_CHECKOUT_NOTICE` 在 `myProgram/` 與 `tests/sales/`
  0 命中（`tests/spec/` 歷史檔允許殘留，不碰）。
- [ ] **Step 2.7**：`python -m pytest tests/sales/` → `509 passed`
- [ ] **Step 2.8**：`python -m tests.perf.bench_sales --smoke` → `smoke：7 場景走向全數符合預期`
- [ ] **Step 2.9**：

```bash
git add myProgram/sales/constants/timing.py tests/sales/test_constants.py tests/sales/test_states.py
git commit -m "refactor(sales): drop dead AUTO_CHECKOUT_NOTICE constant and its test guards"
git branch --contains HEAD
```

---

## 完成回報

4-status ＋ 2 commit SHA ＋ 各 task pytest 末行 ＋ Step 2.6 grep 結果 ＋ Step 2.8 smoke
末行 ＋ TaskList 摘要。
