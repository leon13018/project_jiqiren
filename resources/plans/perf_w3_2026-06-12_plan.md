# perf_w3 實作計畫（plan — HOW）

> **執行者**：sales-coder。對應 spec：`resources/specs/perf_w3_2026-06-12_spec.md`。
> F-6 走 TDD（RED→GREEN）；call site 替換純重構（綠→改→綠）。每 task 一 commit；
> 任一步 pytest 數字不符即停下回報。每 commit 後 `git branch --contains HEAD` 自驗。

**Goal**：F-6 三處數量分類收斂進 `cart.classify_qty`（+5 測試）＋F-7 `_cancel_gate` helper，行為零改變。

---

## Task 0：基線驗證

- [ ] **Step 0.1**：`python -m pytest tests/sales/` → 預期 `504 passed`。

---

## Task 1（F-6）：cart.classify_qty＋三 call site

**Files**：Modify `myProgram/sales/cart.py`、`tests/sales/test_cart.py`、
`myProgram/sales/states/_l2_l3_qty_followup.py`、`myProgram/sales/states/_invalid_qty_reask.py`

- [ ] **Step 1.1（RED）**：`tests/sales/test_cart.py` 檔尾新增：

```python
# ============================================================
# perf_w3 F-6：classify_qty 四分類（收斂三處手寫副本的新 helper）
# ============================================================

## CART-CQ-001
### Scenario: 空 cart、合法數量 → "ok"
def test_classify_qty_ok() -> None:
    c = new_cart()
    assert cart_module.classify_qty(c, "冰紅茶", 3) == "ok"


## CART-CQ-002
### Scenario: qty == 0 → "zero"（顧客明確說 0）
def test_classify_qty_zero() -> None:
    c = new_cart()
    assert cart_module.classify_qty(c, "冰紅茶", 0) == "zero"


## CART-CQ-003
### Scenario: qty 超過剩餘容量 → "over_limit"
def test_classify_qty_over_limit() -> None:
    c = new_cart()
    add_item(c, "冰紅茶", MAX_QTY_PER_ITEM - 2)
    assert cart_module.classify_qty(c, "冰紅茶", 3) == "over_limit"


## CART-CQ-004
### Scenario: cart 已達上限 → "at_cap" 最高優先（與 qty 無關，含 qty==0）
def test_classify_qty_at_cap_overrides_zero() -> None:
    c = new_cart()
    add_item(c, "冰紅茶", MAX_QTY_PER_ITEM)
    assert cart_module.classify_qty(c, "冰紅茶", 0) == "at_cap"
    assert cart_module.classify_qty(c, "冰紅茶", 1) == "at_cap"


## CART-CQ-005
### Scenario: 邊界 — qty 恰等於剩餘容量 → "ok"
def test_classify_qty_boundary_exact_remaining() -> None:
    c = new_cart()
    add_item(c, "冰紅茶", MAX_QTY_PER_ITEM - 5)
    assert cart_module.classify_qty(c, "冰紅茶", 5) == "ok"
```

（test_cart.py 既有 import 已含 `cart_module` / `new_cart` / `add_item` /
`MAX_QTY_PER_ITEM`，不需加 import。）

- [ ] **Step 1.2**：`python -m pytest tests/sales/test_cart.py -q` → 預期 **5 failed**
  （`AttributeError: ... no attribute 'classify_qty'`），既有 12 passed。

- [ ] **Step 1.3（GREEN）**：`cart.py` 的 `remaining_capacity` 之後插入 spec §2-1 的
  `classify_qty` 全文（逐字照抄 spec）。

- [ ] **Step 1.4**：`python -m pytest tests/sales/test_cart.py -q` → `17 passed`。

- [ ] **Step 1.5**：`_l2_l3_qty_followup.py` Pass 1 替換

舊：

```python
        remaining = cart_module.remaining_capacity(cart, product)
        unit = PRODUCTS[product]["單位"]
        if remaining <= 0:
            # cart 內已達上限 → 完全 skip + speak 通知（at-cap 保留既有行為，不進重問鏈）
            io.speak(AT_CAP_NOTICE_TEMPLATE.format(product=product, max_qty=MAX_QTY_PER_ITEM, unit=unit))
            continue
        if qty == 0:
            # 2026-06-09：qty==0 比照超量，收進 invalid_pending 走 Pass 1.5 重問（不假性加入）
            invalid_pending[product] = "zero"
            continue
        if qty > remaining:
            # 2026-06-09：不再 cap，收進 invalid_pending 走 Pass 1.5 合併重問
            invalid_pending[product] = "over_limit"
            continue
        # 正常加入
        cart_module.add_item(cart, product, qty)
        added_count += 1
```

新：

```python
        verdict = cart_module.classify_qty(cart, product, qty)
        unit = PRODUCTS[product]["單位"]
        if verdict == "at_cap":
            # cart 內已達上限 → 完全 skip + speak 通知（at-cap 保留既有行為，不進重問鏈）
            io.speak(AT_CAP_NOTICE_TEMPLATE.format(product=product, max_qty=MAX_QTY_PER_ITEM, unit=unit))
            continue
        if verdict != "ok":
            # 2026-06-09：qty==0（"zero"）/ 超量（"over_limit"）皆不 cap，
            # verdict 字面即 pending reason，收進 invalid_pending 走 Pass 1.5 合併重問
            invalid_pending[product] = verdict
            continue
        # 正常加入
        cart_module.add_item(cart, product, qty)
        added_count += 1
```

- [ ] **Step 1.6**：`_l2_l3_qty_followup.py` sub-loop 替換（首三行 Wave 4 hotfix 註解保留）

舊：

```python
            remaining = cart_module.remaining_capacity(cart, product)
            if remaining <= 0:
                # cart 內已達上限 → 無法再加，即時 speak 提示 + skip 此商品（非 cancel UX，不拼接）
                io.speak(AT_CAP_NOTICE_TEMPLATE.format(product=product, max_qty=MAX_QTY_PER_ITEM, unit=unit))
                return False, None, None
            if qty == 0 or qty > remaining:
                # 2026-06-09：qty==0 / 超量皆不 cap，funnel 進 invalid_qty_reask（單商品）
                reason = "zero" if qty == 0 else "over_limit"
                control = invalid_qty_reask(
                    {product: reason}, cart, io.speak, io.print_terminal,
                    io.read_customer_input, io.speak_and_wait,
                )
```

新：

```python
            verdict = cart_module.classify_qty(cart, product, qty)
            if verdict == "at_cap":
                # cart 內已達上限 → 無法再加，即時 speak 提示 + skip 此商品（非 cancel UX，不拼接）
                io.speak(AT_CAP_NOTICE_TEMPLATE.format(product=product, max_qty=MAX_QTY_PER_ITEM, unit=unit))
                return False, None, None
            if verdict != "ok":
                # 2026-06-09：qty==0 / 超量皆不 cap，funnel 進 invalid_qty_reask（單商品；
                # verdict 字面即 reason）
                control = invalid_qty_reask(
                    {product: verdict}, cart, io.speak, io.print_terminal,
                    io.read_customer_input, io.speak_and_wait,
                )
```

（後續 `if control == "resolved": ...` 至 `return True, None, None` 全部不動。）

- [ ] **Step 1.7**：`_invalid_qty_reask.py` `_classify_into_pending` 替換

舊：

```python
def _classify_into_pending(product: str, qty: int, pending: dict, cart) -> None:
    """重答後重新分類單一商品：合法→add+del；仍 0→reason=zero；仍超量→reason=over_limit。"""
    remaining = cart_module.remaining_capacity(cart, product)
    if 0 < qty <= remaining:
        cart_module.add_item(cart, product, qty)
        del pending[product]
    elif qty == 0:
        pending[product] = "zero"
    else:  # qty > remaining
        pending[product] = "over_limit"
```

新：

```python
def _classify_into_pending(product: str, qty: int, pending: dict, cart) -> None:
    """重答後重新分類單一商品：合法→add+del；仍 0→reason=zero；仍超量→reason=over_limit。"""
    verdict = cart_module.classify_qty(cart, product, qty)
    if verdict == "ok":
        cart_module.add_item(cart, product, qty)
        del pending[product]
        return
    # "at_cap" 在 reask 語境不可達（pending 商品於 Pass 1 已保證 remaining>0，
    # 重問期間該商品唯一加量路徑是成功 add — 隨即移出 pending）；
    # 防禦性映射為 "over_limit" 對齊舊 else 分支，不可達態也不改行為
    pending[product] = "over_limit" if verdict == "at_cap" else verdict
```

- [ ] **Step 1.8**：`python -m pytest tests/sales/` → **`509 passed`**
- [ ] **Step 1.9**：

```bash
git add myProgram/sales/cart.py tests/sales/test_cart.py myProgram/sales/states/_l2_l3_qty_followup.py myProgram/sales/states/_invalid_qty_reask.py
git commit -m "refactor(sales): unify qty classification via cart.classify_qty"
git branch --contains HEAD
```

---

## Task 2（F-7）：_cancel_gate helper

**Files**：Modify `myProgram/sales/states/l2_l3_dialog.py`

- [ ] **Step 2.1**：`_dialog_checkout_confirm` 定義之前插入 spec §2-2 的 `_cancel_gate`
  全文（逐字照抄 spec）。

- [ ] **Step 2.2**：`_dialog_checkout_confirm` cancel 分支替換（前方 2026-05-29／05-30
  兩段解釋註解整段保留）

舊：

```python
        if is_cancel_intent(response):
            if CANCEL_CONFIRM.run(io):
                return "cancel_to_l1"
            io.speak(CANCEL_DECLINED_NOTICE)
            io.speak(prompt)
            continue
```

新：

```python
        if is_cancel_intent(response):
            if _cancel_gate(io, prompt):
                return "cancel_to_l1"
            continue
```

- [ ] **Step 2.3**：`_dialog_unclear_final_confirmation` 退出分支替換（前方 cross-L
  cancel 兩行註解保留；NO 行為註解併入 helper docstring，原 `# NO → ...` 行刪除）

舊：

```python
        if intent == "退出交易":
            # 2026-05-29 cross-L cancel：unclear final 內語音退出 intent → 先過 cancel_confirm gate
            # （終端 "1" / silent timeout / unclear exhausted 仍直接退，不 gate — 那些是介面操作非 cancel intent）
            if CANCEL_CONFIRM.run(io):
                return session.exit_a()
            # NO → speak DECLINED + 重 prompt unclear final + continue（不計 unclear_count）
            io.speak(CANCEL_DECLINED_NOTICE)
            io.speak(L3_UNCLEAR_FINAL_PROMPT)
            continue
```

新：

```python
        if intent == "退出交易":
            # 2026-05-29 cross-L cancel：unclear final 內語音退出 intent → 先過 cancel_confirm gate
            # （終端 "1" / silent timeout / unclear exhausted 仍直接退，不 gate — 那些是介面操作非 cancel intent）
            if _cancel_gate(io, L3_UNCLEAR_FINAL_PROMPT):
                return session.exit_a()
            continue
```

- [ ] **Step 2.4**：grep 自驗：`CANCEL_DECLINED_NOTICE` 在 `l2_l3_dialog.py` 只剩
  `_cancel_gate` 內 1 處使用（import 行不計）。
- [ ] **Step 2.5**：`python -m pytest tests/sales/` → `509 passed`
- [ ] **Step 2.6**：`python -m tests.perf.bench_sales --smoke` → `smoke：7 場景走向全數符合預期`
- [ ] **Step 2.7**：

```bash
git add myProgram/sales/states/l2_l3_dialog.py
git commit -m "refactor(sales): extract shared cancel-gate tail for confirm substates"
git branch --contains HEAD
```

---

## 完成回報

4-status ＋ 2 commit SHA ＋ 各 task pytest 末行（Step 1.2 RED 末行也要附）＋
Step 2.6 smoke 末行 ＋ TaskList 摘要。
