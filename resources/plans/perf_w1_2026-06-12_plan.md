# perf_w1 實作計畫（plan — HOW）

> **執行者**：sales-coder。對應 spec：`resources/specs/perf_w1_2026-06-12_spec.md`。
> 純重構規約：綠 → 改 → 綠 → commit；每 task 一 commit；任一步 pytest 不是 `504 passed` 即停下回報。
> 每 commit 後 `git branch --contains HEAD` 自驗落 `worktree-perf-w1`。

**Goal**：F-9a 搬檔＋F-1 預 lower＋F-1b KG 化＋F-2 預編譯＋F-3 product 預 lower，行為零改變。

---

## Task 0：基線驗證

- [ ] **Step 0.1**：`python -m pytest tests/sales/` → 預期 `504 passed`。

---

## Task 1（F-9a＋F-11）：keyword_group 搬進 constants/

**Files**：Move `myProgram/sales/keyword_group.py` → `myProgram/sales/constants/keyword_group.py`；Modify `myProgram/sales/nlu.py`、`myProgram/sales/constants/keywords.py`、`tests/sales/test_keyword_group.py`、兩層 code_map、`constants/CLAUDE.md`

- [ ] **Step 1.1**：`git mv myProgram/sales/keyword_group.py myProgram/sales/constants/keyword_group.py`

- [ ] **Step 1.2**：Read 新路徑檔，docstring（1-12 行）整段替換為：

```python
"""KeywordGroup — keyword 雙集封裝＋比對原語（W1 oop_w1；2026-06-12 perf_w1 搬入 constants/）。

substring 比對集負責長詞安全命中，嚴格相等集（strict-short）負責短單字
（如「好/取消/繼續」），**防短詞 substring 誤命中**（「好」中「好亂」、「取消」中「取消會議」）。

定位：純值原語（只 import stdlib），屬資料層——本層 keywords.py 以 KeywordGroup
建 KG_* 配對實例；nlu / states 經 `.matches` 消費。搬入理由：消除
constants（資料層）→ sales/（邏輯層）的唯一反向 import（findings F-9a）。
"""
```

- [ ] **Step 1.3**：`nlu.py` import 段（32-34 行）

舊：

```python
# 比對原語本體已搬至 keyword_group.py（W1）；re-export 保既有
# `from myProgram.sales.nlu import contains_any` 的 caller / 測試相容
from myProgram.sales.keyword_group import KeywordGroup, contains_any, equals_strict_short
```

新（過時 re-export 聲明刪除；原語名暫留——Task 3 收窄）：

```python
from myProgram.sales.constants.keyword_group import KeywordGroup, contains_any, equals_strict_short
```

- [ ] **Step 1.4**：`constants/keywords.py`（14-15 行）

舊：

```python
# W1 oop_w1：KeywordGroup 雙集封裝（類別本體在 keyword_group.py，本檔只建配對實例）
from myProgram.sales.keyword_group import KeywordGroup
```

新：

```python
# W1 oop_w1：KeywordGroup 雙集封裝（類別本體在本層 keyword_group.py，本檔只建配對實例）
from myProgram.sales.constants.keyword_group import KeywordGroup
```

- [ ] **Step 1.5**：`tests/sales/test_keyword_group.py:10`

舊：`from myProgram.sales.keyword_group import KeywordGroup`
新：`from myProgram.sales.constants.keyword_group import KeywordGroup`

- [ ] **Step 1.6**：`myProgram/sales/.claude/code_map.md`
  - 刪整行：`- \`keyword_group.py\` — \`KeywordGroup\` 雙集封裝（substring + strict-short）+ 比對原語 \`contains_any\` / \`equals_strict_short\` 本體（OOP 重構 W1）。`
  - `nlu.py` 行的「比對原語自 `keyword_group.py` re-export」改「比對經 `constants/keyword_group.py` 的 `KeywordGroup`」。
  - `constants/` 子目錄行清單尾補 ` / keyword_group`。

- [ ] **Step 1.7**：`myProgram/sales/constants/.claude/code_map.md`
  - `keywords.py` 行尾「（類別在 `sales/keyword_group.py`）」改「（類別在本層 `keyword_group.py`）」，並在該行 NLU 關鍵字描述補 `＋HAWK_SLOGANS 叫賣標語`。
  - 「其他常數」段新增一行：`- \`keyword_group.py\` — \`KeywordGroup\` 雙集封裝＋比對原語 \`contains_any\`/\`equals_strict_short\`（純值原語，只 import stdlib；2026-06-12 perf_w1 自 sales/ 搬入）。`
  - **真 drift 修正（F-11）**：`l1_text.py` 行「（叫賣標語 `HAWK_SLOGANS` 等）」改「（選單 / 進入提示）」——`HAWK_SLOGANS` 實際定義於 `keywords.py:65`。

- [ ] **Step 1.8**：`myProgram/sales/constants/CLAUDE.md` 首段「純資料，無邏輯」句尾補：「（例外：`keyword_group.py` 為純值比對原語，只 import stdlib）」。

- [ ] **Step 1.9**：`python -m pytest tests/sales/` → `504 passed`
- [ ] **Step 1.10**：

```bash
git add myProgram/sales/keyword_group.py myProgram/sales/constants/keyword_group.py myProgram/sales/nlu.py myProgram/sales/constants/keywords.py tests/sales/test_keyword_group.py myProgram/sales/.claude/code_map.md myProgram/sales/constants/.claude/code_map.md myProgram/sales/constants/CLAUDE.md
git commit -m "refactor(sales): relocate keyword_group into constants package"
git branch --contains HEAD
```

---

## Task 2（F-1）：KeywordGroup 預 lower

**Files**：Modify `myProgram/sales/constants/keyword_group.py`

- [ ] **Step 2.1**：類別整段替換

舊：

```python
@dataclass(frozen=True)
class KeywordGroup:
    """keyword 雙集封裝：substring 比對集 + 嚴格相等集（防短詞 substring 誤命中）。"""
    substrings: tuple
    strict_short: tuple = ()

    def matches(self, text: str) -> bool:
        return contains_any(text, self.substrings) or equals_strict_short(text, self.strict_short)
```

新：

```python
@dataclass(frozen=True)
class KeywordGroup:
    """keyword 雙集封裝：substring 比對集 + 嚴格相等集（防短詞 substring 誤命中）。

    perf_w1 F-1：keyword 為 frozen 常數，建構時預先小寫化（tuple / frozenset），
    熱路徑 matches 不再每呼叫重算 kw.lower() 或重建 list。
    等價性：text.strip().lower() == text.lower().strip()（lower 不增減空白）；
    eq / repr 只看 dataclass 欄位，預算屬性不影響既有斷言。
    """
    substrings: tuple
    strict_short: tuple = ()

    def __post_init__(self):
        # frozen dataclass 加非欄位屬性須走 object.__setattr__
        object.__setattr__(self, "_substrings_lower",
                           tuple(kw.lower() for kw in self.substrings))
        object.__setattr__(self, "_strict_short_lower",
                           frozenset(kw.lower() for kw in self.strict_short))

    def matches(self, text: str) -> bool:
        text_lower = text.lower()
        return (any(kw in text_lower for kw in self._substrings_lower)
                or text_lower.strip() in self._strict_short_lower)
```

（`contains_any` / `equals_strict_short` 兩個模組函式**不動**。）

- [ ] **Step 2.2**：`python -m pytest tests/sales/` → `504 passed`
- [ ] **Step 2.3**：

```bash
git add myProgram/sales/constants/keyword_group.py
git commit -m "perf(sales): precompute lowercased keyword sets in KeywordGroup"
git branch --contains HEAD
```

---

## Task 3（F-1b）：nlu classify_intent 呼叫點 KG 化

**Files**：Modify `myProgram/sales/nlu.py`

- [ ] **Step 3.1**：`_KEYWORDS_EXIT = [...]`（~110 行）之後插入：

```python
# ============================================================
# 模組級 KG 實例（perf_w1 F-1b）：classify_intent 熱路徑統一走預 lower 的
# KeywordGroup.matches；list 常數保留為單一事實來源（測試直接驗 list）。
# 單集者另一側留空：純 substring → strict_short 預設 ()；純 strict → substrings=()。
# ============================================================
_KG_REJECT_L3_STRICT = KeywordGroup(tuple(_KEYWORDS_REJECT_L3_STRICT))
_KG_THINK = KeywordGroup(tuple(_KEYWORDS_THINK))
_KG_CHECKOUT = KeywordGroup(tuple(_KEYWORDS_CHECKOUT))
_KG_SERVICE = KeywordGroup(tuple(_KEYWORDS_SERVICE))
_KG_CONTINUE = KeywordGroup(tuple(_KEYWORDS_CONTINUE))
_KG_EXIT = KeywordGroup(tuple(_KEYWORDS_EXIT))
_KG_ICED_TEA = KeywordGroup(tuple(KEYWORDS_ICED_TEA))
_KG_SCRATCH = KeywordGroup(tuple(KEYWORDS_SCRATCH))
_KG_L4_ACK = KeywordGroup(tuple(KEYWORDS_L4_ACK_OR_WAIT), tuple(KEYWORDS_L4_ACK_SHORT))
_KG_WANT_TO_BUY = KeywordGroup(tuple(KEYWORDS_WANT_TO_BUY_VAGUE), tuple(KEYWORDS_WANT_TO_BUY_SHORT))
_KG_NO_NOPE = KeywordGroup((), ("no", "nope"))
```

注意：`_KG_REJECT_L3_STRICT` 須在 `_KEYWORDS_REJECT_L3_STRICT` 定義之後——本插入點（~110 行）滿足。

- [ ] **Step 3.2**：l4_service 三判定（既有註解行全保留）

舊：

```python
        if contains_any(text, _KEYWORDS_CONTINUE):
            return "繼續交易"
        if contains_any(text, _KEYWORDS_EXIT):
            return "退出交易"
        if equals_strict_short(text, ["no", "nope"]):
            return "退出交易"
```

新：

```python
        if _KG_CONTINUE.matches(text):
            return "繼續交易"
        if _KG_EXIT.matches(text):
            return "退出交易"
        if _KG_NO_NOPE.matches(text):
            return "退出交易"
```

- [ ] **Step 3.3**：l4 mode 等待安撫（前方 195-197 三行註解保留）

舊：

```python
    if mode == "l4":
        if equals_strict_short(text, KEYWORDS_L4_ACK_SHORT):
            return "等待安撫"
        if contains_any(text, KEYWORDS_L4_ACK_OR_WAIT):
            return "等待安撫"
```

新（兩判定同回值，OR 語義合一）：

```python
    if mode == "l4":
        if _KG_L4_ACK.matches(text):
            return "等待安撫"
```

- [ ] **Step 3.4**：l2/l4 no/nope（206-208；前方兩行註解保留）

舊：

```python
    if mode in ("l2", "l4"):
        if equals_strict_short(text, ["no", "nope"]):
            return "拒絕"
```

新：

```python
    if mode in ("l2", "l4"):
        if _KG_NO_NOPE.matches(text):
            return "拒絕"
```

- [ ] **Step 3.5**：normal mode L3 嚴格判定（前方三行註解保留）

舊：`        if contains_any(text, _KEYWORDS_REJECT_L3_STRICT):`
新：`        if _KG_REJECT_L3_STRICT.matches(text):`

- [ ] **Step 3.6**：通用優先序五判定

舊：

```python
    if contains_any(text, _KEYWORDS_THINK):
        return "想一下"
    if contains_any(text, _KEYWORDS_CHECKOUT):
        return "結帳"
    if contains_any(text, _KEYWORDS_SERVICE):
        return "客服"
    if contains_any(text, KEYWORDS_ICED_TEA):
        return "商品:冰紅茶"
    if contains_any(text, KEYWORDS_SCRATCH):
        return "商品:刮刮樂"
```

新：

```python
    if _KG_THINK.matches(text):
        return "想一下"
    if _KG_CHECKOUT.matches(text):
        return "結帳"
    if _KG_SERVICE.matches(text):
        return "客服"
    if _KG_ICED_TEA.matches(text):
        return "商品:冰紅茶"
    if _KG_SCRATCH.matches(text):
        return "商品:刮刮樂"
```

- [ ] **Step 3.7**：想買無商品（233-236 四行註解保留）

舊：

```python
    if mode in ("l2", "normal"):
        if equals_strict_short(text, KEYWORDS_WANT_TO_BUY_SHORT):
            return "想買無商品"
        if contains_any(text, KEYWORDS_WANT_TO_BUY_VAGUE):
            return "想買無商品"
```

新（兩判定同回值，OR 語義合一）：

```python
    if mode in ("l2", "normal"):
        if _KG_WANT_TO_BUY.matches(text):
            return "想買無商品"
```

- [ ] **Step 3.8**：`_invalid_qty_reask.py` 改直取原語（消 nlu re-export 中繼；2026-06-12 BLOCKED 裁決 B）

舊：

```python
from myProgram.sales.nlu import (
    has_quantity, parse_quantity, classify_intent,
    contains_any,
)
```

新：

```python
from myProgram.sales.nlu import has_quantity, parse_quantity, classify_intent
from myProgram.sales.constants.keyword_group import contains_any
```

- [ ] **Step 3.8b**：nlu import 收窄（classify_intent 已無原語直呼、唯一外部 caller 已改直取）

舊：`from myProgram.sales.constants.keyword_group import KeywordGroup, contains_any, equals_strict_short`
新：`from myProgram.sales.constants.keyword_group import KeywordGroup`

- [ ] **Step 3.9**：grep 自驗：`contains_any|equals_strict_short` 在 `myProgram/sales/nlu.py` 應 0 命中。
- [ ] **Step 3.10**：`python -m pytest tests/sales/` → `504 passed`
- [ ] **Step 3.11**：

```bash
git add myProgram/sales/nlu.py myProgram/sales/states/_invalid_qty_reask.py
git commit -m "perf(sales): route classify_intent matching through prebuilt KeywordGroups"
git branch --contains HEAD
```

---

## Task 4（F-2）：nlu regex／translate table 預編譯

**Files**：Modify `myProgram/sales/nlu.py`

- [ ] **Step 4.1**：Task 3 的 KG 區塊之後插入：

```python
# ============================================================
# 預編譯 regex / translate table（perf_w1 F-2）：pattern 內容恆定，
# 模組載入算一次；省每呼叫 f-string 插值＋re cache lookup＋maketrans 重建。
# ============================================================
_CTRL_CHARS_RE = re.compile(r"[\x00-\x08\x0b\x0c\x0e-\x1f\x7f]")
_FULLWIDTH_DIGIT_TABLE = str.maketrans("０１２３４５６７８９", "0123456789")
_NEG_CONTINUE_RE = re.compile(r"[不別沒休][一-龥]{0,5}繼續")
_ARABIC_DIGITS_RE = re.compile(r"\d+")
```

- [ ] **Step 4.2**：`_CHINESE_UNIT_CHARS = "..."` 定義行之後插入：

```python
# 複合中文數字 regex（perf_w1 F-2 預編譯；pattern 與原 f-string 逐字相同）
_TENS_RE = re.compile(rf"([{_CHINESE_UNIT_CHARS}])?[十拾]([{_CHINESE_UNIT_CHARS}])?")
_HUNDREDS_RE = re.compile(rf"([{_CHINESE_UNIT_CHARS}])[百佰]([{_CHINESE_UNIT_CHARS}十拾]*)?")
```

- [ ] **Step 4.3**：六個呼叫點替換（各行前後註解不動）：

| 位置 | 舊 | 新 |
|---|---|---|
| normalize_input | `text = re.sub(r"[\x00-\x08\x0b\x0c\x0e-\x1f\x7f]", "", text)` | `text = _CTRL_CHARS_RE.sub("", text)` |
| normalize_input | `text = text.translate(str.maketrans("０１２３４５６７８９", "0123456789"))` | `text = text.translate(_FULLWIDTH_DIGIT_TABLE)` |
| classify_intent l4_service | `if re.search(r"[不別沒休][一-龥]{0,5}繼續", text):` | `if _NEG_CONTINUE_RE.search(text):` |
| `_match_tens` | `m = re.search(rf"([{_CHINESE_UNIT_CHARS}])?[十拾]([{_CHINESE_UNIT_CHARS}])?", text)` | `m = _TENS_RE.search(text)` |
| `_parse_compound_chinese` | `m = re.search(rf"([{units}])[百佰]([{units}十拾]*)?", text)` | `m = _HUNDREDS_RE.search(text)` |
| `has_quantity` / `parse_quantity` | `re.search(r"\d+", text)` / `re.findall(r"\d+", text)` | `_ARABIC_DIGITS_RE.search(text)` / `_ARABIC_DIGITS_RE.findall(text)` |

- [ ] **Step 4.4**：`_parse_compound_chinese` 內 `units = _CHINESE_UNIT_CHARS` local 已無使用 → 刪除該行（百位 regex 已預編譯）。
- [ ] **Step 4.5**：grep 自驗：`re\.(search|sub|findall)\(r` 在 nlu.py 應 0 命中（`import re` 保留——compile 用）。
- [ ] **Step 4.6**：`python -m pytest tests/sales/` → `504 passed`
- [ ] **Step 4.7**：

```bash
git add myProgram/sales/nlu.py
git commit -m "perf(sales): precompile nlu regexes and fullwidth translate table"
git branch --contains HEAD
```

---

## Task 5（F-3）：product_parser 預 lower

**Files**：Modify `myProgram/sales/product_parser.py`

- [ ] **Step 5.1**：`_PRODUCT_KEYWORD_TO_NAME` list 定義之後插入：

```python
# 預算 (kw_lower, kw_len, product)（perf_w1 F-3）：keyword 為 frozen 常數，
# 每呼叫重算 lower / len 是浪費。原表保留為單一事實來源。
_PRODUCT_KEYWORDS_PRE: list = [
    (keyword.lower(), len(keyword), product)
    for keyword, product in _PRODUCT_KEYWORD_TO_NAME
]
```

- [ ] **Step 5.2**：掃描迴圈替換

舊：

```python
    for keyword, product in _PRODUCT_KEYWORD_TO_NAME:
        kw_lower = keyword.lower()
        pos = 0
        while True:
            idx = text_lower.find(kw_lower, pos)
            if idx == -1:
                break
            end = idx + len(keyword)
```

新：

```python
    for kw_lower, kw_len, product in _PRODUCT_KEYWORDS_PRE:
        pos = 0
        while True:
            idx = text_lower.find(kw_lower, pos)
            if idx == -1:
                break
            end = idx + kw_len
```

（迴圈其餘行——overlaps 判定、found/occupied append、`pos = end`——不動。）

- [ ] **Step 5.3**：`python -m pytest tests/sales/` → `504 passed`
- [ ] **Step 5.4**：`python -m tests.perf.bench_sales --smoke` → `smoke：7 場景走向全數符合預期`
- [ ] **Step 5.5**：

```bash
git add myProgram/sales/product_parser.py
git commit -m "perf(sales): precompute lowercased product keywords"
git branch --contains HEAD
```

---

## 完成回報

4-status ＋ 5 commit SHA ＋ 各 task pytest 末行 ＋ Step 5.4 smoke 末行 ＋ TaskList 摘要。
