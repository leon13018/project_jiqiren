# L5：謝謝惠顧（致謝層）

> **角色：顧客。** 結帳完成的最後一步。最簡單的一層 — 只有一條鏈路、無分支、無顧客互動。

---

## 入口（從何處進來）

| 來源 | 條件 |
|---|---|
| L4 鏈路 A | 終端輸入 `s`（掃碼成功）|

---

## 進入時動作

- **進入時立即啟動 OpenCV mute**（屏蔽至子例程 A 啟動 — 致謝 `THANK_DELAY`(3s) + 子例程 A `OPENCV_MUTE`(12s) = 共 15s 總屏蔽期），避免下一個顧客在致謝期間誤觸 L2
- **[語音]** `"謝謝您的光臨，歡迎再度光臨"`
- **[動作]** TBD（S3 補；建議揮手 / 鞠躬）
- **清空 `cart`**（交易完成，購物車重置）
- 等待 `THANK_DELAY` 秒（= 3s，見 [L0 時間常數](L0_共通.md#-時間常數)）

> **設計推論（待 review）**：cart 在 L5 進入時清空 — 此時交易已完成（L4 掃碼確認）、cart 內容是「歷史」。若你希望 L4 掃碼成功（鏈路 A）時就清，告訴我。

---

## 邏輯鏈路

### 鏈路 A — 致謝完成 → 回 L1

- **trigger**：自動 — 等待 `THANK_DELAY`（= 3s）過後
- **進入動作**：無
- **轉到**：L1 叫賣，**套用** [L0 子例程 A：回 L1 叫賣](L0_共通.md#子例程-a回-l1-叫賣)

> 此層**無顧客互動**：3s 等待期間不接受任何顧客輸入 / 不解析關鍵字（除全域 `q` 退出）。OpenCV 此期間不偵測，下一個顧客至少要等 15s（致謝 + mute）才會被招呼，符合禮貌間隔。

---

## 出口列表

| 出口 | 觸發條件 |
|---|---|
| L1 叫賣（套子例程）| 3s 等待過後（自動）|
| 程式終止 | 全域 `q` |

---

## 變更紀錄

- **2026-05-24 改寫**：套用統一模板格式
- **設計推論（待 review）**：cart 在 L5 進入時清空（vs L4 掃碼成功時清空 — 兩種都合理）
- 釐清點：本層**無待釐清點**（原文最清晰簡單的一層）
- **2026-05-24 中優先 17 項收尾**：進入時立即啟動 OpenCV mute（致謝 3s + 子例程 A mute 12s = 15s 總屏蔽），防止下一顧客在致謝期間誤觸 L2。
