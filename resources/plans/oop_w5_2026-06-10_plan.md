# oop_w5 — 實作 plan（HOW，step-by-step）

> 對應 spec：`resources/specs/oop_w5_2026-06-10_spec.md`（§1 前置盤查 + §2-2 表是行為權威）。
> 全 suite = `python -m pytest tests/sales/ -v --tb=short`。
> 工作目錄：worktree `.claude/worktrees/oop-w5/`，branch `worktree-oop-w5`。
> **Wave 6 招生效**。鐵則：晚綁定 `states.run_*`（禁 from-import 捕引用）；kwargs 逐字照抄現行 logic.py。

## Commit 1 — machine.py + 測試

1. **[RED]** 新增 `tests/sales/test_machine.py`（mock seam 與 test_logic 同：`monkeypatch.setattr(states_module, "run_*", stub)`）：
   - `Transition` frozen（賦值 raise FrozenInstanceError）+ `via_subroutine_a` 預設 False + 相等性
   - 各 State 轉移映射（每個一條，stub 對應 `states.run_*`）：
     - `L1State`：stub 回 `"L2"` → `Transition("dialog")`；回 `None` → `None`
     - `DialogState`：stub 回 `("L4", 0)` → `Transition("l4")`；回 `("L1_via_subroutine_a", 0)`（stub 清 cart）→ `Transition("l1", via_subroutine_a=True)`
     - `L4State`：stub 回 `("L5", 0, 0)` → `Transition("l5")`；回 `("L1_via_subroutine_a", 0, 0)`（stub 清 cart）→ `Transition("l1", True)`
     - `L5State`：stub 回 `("L1", 0, 0)`（非標準字串；stub 清 cart）→ 仍 `Transition("l1", True)`（忽略回傳值）
   - `L1State` consume-reset：machine.enter_hawk_immediately=True → run 後 stub 收到 True 且 machine 旗號變 False
   - machine 進場 invariant：cart 非空進 "l1" → `AssertionError` match "Cart invariant 違反" 且含「進 L1」
   - （State 實例經 `SalesMachine(callbacks, cart)` 建出後取 `machine._states[...]` 測，或直接建子類別實例 + 手造 machine——擇可讀者）
2. **[RED 證據]** 跑該檔，FAIL = `ModuleNotFoundError: No module named 'myProgram.sales.states.machine'`。
3. **[GREEN]** 新增 `myProgram/sales/states/machine.py`：
   - module docstring（繁中：State pattern 動機 + 「mock seam = states 模組屬性，全部晚綁定」一句 + A4-c invariant 說明）
   - `import` 區：`from myProgram.sales import states`（模組物件，晚綁定用）、`from myProgram.sales import cart as cart_module`、stdlib
   - `Transition` / `State`（spec §2-1 原文）
   - `_assert_cart_empty` / `_assert_cart_nonempty` 自 logic.py **原樣搬入**（含 docstring；訊息一字不改）
   - 四 State 子類別（spec §2-2 表：kwargs 逐字照抄現行 logic.py 呼叫點；行為性註解搬入）
   - `SalesMachine`（spec §2-3 原文；`states.run_subroutine_a(mute_opencv=...)` 晚綁定；原 `_invoke_subroutine_a` 的簡化歷史註解搬到該行旁）
4. 跑 `test_machine.py` 全 PASS + 全 suite 477 不動（logic.py 未動，machine 尚未接線——此時 machine 是已測試但未接線的新構件，C2 立即接線）。
5. **[COMMIT 1]**（2 檔明列）→ branch 自驗。

## Commit 2 — logic.py facade 化

6. `logic.py`：
   - 刪主迴圈 / `_invoke_subroutine_a` / `_assert_cart_empty` / `_assert_cart_nonempty`
   - `run` 簽名一字不改；body = 組 callbacks dict（14 鍵含 speak_and_wait）+ `return SalesMachine(callbacks=callbacks, cart=cart_module.new_cart()).run()`
   - import 更新：`from myProgram.sales.states.machine import SalesMachine`；移除不再用的 `states` import（grep 檔內確認無他用）
   - module docstring：職責改「facade」；「各 run_? return shape」段保留；「L1 入口流程」說明保留於 run docstring
7. 全 suite 全綠——**test_logic 6 個全綠 = 等價性核心證明**（它們 stub 的 5 個 states 模組屬性 seam + 嚴格 kwargs + 旗號序列 [False,True,True] 全走新 machine 路徑）。
8. 傘狀 `resources/specs/oop_refactor_2026-06-10_design.md` §4-5 對齊（`entry_invariant`/`entry_ctx`、`Transition | None`、callbacks dict 不入 DialogIO 一句）。
9. **[COMMIT 2]**（2 檔明列）→ branch 自驗。
10. 收斂 grep：`_invoke_subroutine_a|_assert_cart_empty|_assert_cart_nonempty` 在 `myProgram/` 僅剩 machine.py 定義處；`logic.py` 內無 `while True`。

## 回報格式

- 首行 4-status；2 commit SHA + branch 驗證；RED 證據（step 2）
- 全 suite 最終數字 + 尾 30 行
- 晚綁定自證（machine.py 內所有 `states.` 呼叫點行號列表）
- step 10 收斂 grep 結果
- 觸發 bug 修理指示與否
