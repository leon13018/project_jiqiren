# Pi 端待辦 — bugfix 複測（千/萬 數量 + 商品名歪+數量同句）

- **建立日期**：2026-06-14
- **對應提交**：`4c025ad`（千/萬）+ `3b5797f`（拆句糾錯）
- **簡介**：修 Pi 實測兩 bug。**無新套件**（純解析 / dispatch 邏輯）。只需複測確認修好。

---

## 複測案例（`python3.11 -m myProgram` → 叫賣 → L2/L3）

| Bug | 講法 | 修前 | 修後期望 |
|---|---|---|---|
| 1 | 問數量時答「一千張」 | 靜默加 **1 張** | `parse_quantity`=1000 → 超量 → **「最多只能選購 50」** reask |
| 2 | 問商品時講「刮樂一千張」 | unclear（無法識別） | 拆「刮樂│一千張」→ 糾「刮刮樂」→ 數量 1000 → 超量 reask |
| 1+2 | 「刮樂三張」「刮樂兩張」 | （視情況） | 刮刮樂 ×3 / ×2（拆句 + 正常數量） |

**回歸確認（不可壞）**：一般數量 五張/十張/五十張 正常；商品名正確時（紅茶/刮刮樂）照常。

---

## 已知 latent（非本次修、unreachable，記錄備查）
- 「百張」省略「一」→ `parse_quantity` 回 1（`千張/萬張` 因 regex 前導單位可選而正確回 1000/10000；`百張` 的 `_HUNDREDS_RE` 前導單位必填）。商品名無含「百」字、顧客極少省略「一」直接講「百張」→ 實務碰不到。要修：`_HUNDREDS_RE` 前導單位改可選（一字之差）。

## 驗證
- 「一千張」→ 聽到「最多只能選購 50」= bug1 修好。
- 「刮樂一千張」→ 認成刮刮樂並走超量 reask（非 unclear）= bug2 修好。

**回報**：兩 bug 是否如期修好 + 有無新異常。
