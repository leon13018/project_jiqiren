# Pi 端待辦 — 拼音糾錯 Phase B 問商品環節（實測 + 調閾值）

- **建立日期**：2026-06-14
- **對應提交**：`d77600c`（①內嵌數量）+ `56a5279`（②引擎擴展）+ `c798939`（②問商品掛載）
- **簡介**：問商品環節兩個糾錯掛載點上線。**無新套件**（pypinyin 已於 Phase A `d5df8a1` 裝好；本機 Windows 仍 graceful no-op）。本檔只需 Pi 實測效力 + 調閾值。

---

## 實測案例（`python3.11 -m myProgram`，走叫賣 → L2/L3 對話）

| # | 顧客講（ASR 可能聽歪成） | 期望 |
|---|---|---|
| ① 內嵌數量 | 「紅茶**商品**」(紅茶三瓶) | 直接 冰紅茶 ×3，**不再多問一次數量** |
| ② 商品名 | 「**刮樂**」「**尬尬樂**」(刮刮樂) | 認成刮刮樂 → 進數量追問 |
| ② 商品名 | 「**茶**」(紅茶不完整) | 認成冰紅茶 |
| 協同 | 「**刮樂三瓶**」(商品名+數量都歪) | 刮刮樂 ×3（②修商品名→重 parse 觸發①修數量） |

**反向（誤糾防線，務必試）**：問商品時講「**客服**」「**不要**」「**等一下**」「**沒了**」等非商品——**不可**被糾成某商品/數量（應各自正常分流）。誤糾比漏糾更傷 UX。

## 調閾值（`myProgram/sales/phonetic.py` 頂部）
- `SIMILARITY_THRESHOLD = 0.75`、`AMBIGUITY_MARGIN = 0.25`（與 Phase A 共用同兩值）。
- 問商品字數比問數量雜（2~3 字、疊字、子串）→ 若實測**漏糾**多 → 略降；**誤糾**多 → 略升 / 加大 margin。
- 改完跑 `python -m pytest tests/sales/`（注入式單元測試不依賴閾值魔數，行為測試仍應綠）再 push。

---

## 驗證
- ① 「紅茶商品」一句講完直接 ×3、無第二次「要幾瓶」= ① 生效。
- ②「刮樂/尬尬樂/茶」被認成正確商品 = ② 生效。
- 非商品語句不被誤糾 = 安全閥 OK。

**回報**：實測哪些 case 糾對 / 糾錯（誤糾）+ 最終想定的閾值（若有調）。
