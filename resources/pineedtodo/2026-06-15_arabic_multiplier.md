# Pi 端待辦 — 大數 arabic+multiplier 複測

- **建立日期**：2026-06-15
- **對應提交**：`c766253`（parse_quantity 阿拉伯數字+中文乘數）
- **簡介**：補完 `4c025ad` 千/萬 fix 漏的 arabic 路徑（「9萬」原靜默變 9）。**無新套件**，只需複測。

---

## 複測

| 講法 | 修前 | 修後期望 |
|---|---|---|
| 問數量時答「9萬瓶」/「1萬」 | 靜默加 9 / 1 | `parse_quantity`=90000/10000 → 超量 → 「最多只能選購 N 瓶」reask |
| 「3千」 | 靜默加 3 | 3000 → 超量 reask |

**回歸（不可壞）**：純阿拉伯 100瓶/9/15瓶 正常；純中文 三千=3000、一千=1000 正常。

---

## 已知未修（本輪 out of scope，你已選只修大數）
- **數量提前**：「三瓶紅茶」（數量在商品前）仍會多問一次（sticky-right）。
- **合音還原**：「醬就好/將就好→這樣就好」仍不識別（→ unclear）。
- 混合複合「9萬5千」取近似 90000（>50 必超量、reask 不受影響）。

## 驗證
「9萬瓶」→ 聽到「最多只能選購 N 瓶」（非靜默加 9）= 修好。

**回報**：是否如期 + 有無新異常。
